// Generated by `wit-bindgen` 0.57.1. DO NOT EDIT!
//
// This code was generated from the following packages:
//     wasi:clocks@0.3.0
//     wasi:filesystem@0.3.0
//     wasi:sockets@0.3.0
//     wasi:random@0.3.0
//     wasi:cli@0.3.0
//     wasi:http@0.3.0
//     jamesstocktonj1:componentize-sdk-p3

package wasi_filesystem_types

import (
	"github.com/jamesstocktonj1/componentize-sdk/p3/gen/wasi_clocks_system_clock"
	witAsync "go.bytecodealliance.org/pkg/wit/async"
	witRuntime "go.bytecodealliance.org/pkg/wit/runtime"
	witTypes "go.bytecodealliance.org/pkg/wit/types"
	"runtime"
	"unsafe"
)

//go:wasmimport wasi:filesystem/types@0.3.0 [stream-new-0][method]descriptor.read-via-stream
func wasm_stream_new_u8() uint64

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][stream-read-0][method]descriptor.read-via-stream
func wasm_stream_read_u8(handle int32, item unsafe.Pointer, count uint32) uint32

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][stream-write-0][method]descriptor.read-via-stream
func wasm_stream_write_u8(handle int32, item unsafe.Pointer, count uint32) uint32

//go:wasmimport wasi:filesystem/types@0.3.0 [stream-drop-readable-0][method]descriptor.read-via-stream
func wasm_stream_drop_readable_u8(handle int32)

//go:wasmimport wasi:filesystem/types@0.3.0 [stream-drop-writable-0][method]descriptor.read-via-stream
func wasm_stream_drop_writable_u8(handle int32)

var wasm_stream_vtable_u8 = witTypes.StreamVtable[uint8]{
	1,
	1,
	wasm_stream_read_u8,
	wasm_stream_write_u8,
	nil,
	nil,
	wasm_stream_drop_readable_u8,
	wasm_stream_drop_writable_u8,
	nil,
	nil,
}

func MakeStreamU8() (*witTypes.StreamWriter[uint8], *witTypes.StreamReader[uint8]) {
	pair := wasm_stream_new_u8()
	return witTypes.MakeStreamWriter[uint8](&wasm_stream_vtable_u8, int32(pair>>32)),
		witTypes.MakeStreamReader[uint8](&wasm_stream_vtable_u8, int32(pair&0xFFFFFFFF))
}

func LiftStreamU8(handle int32) *witTypes.StreamReader[uint8] {
	return witTypes.MakeStreamReader[uint8](&wasm_stream_vtable_u8, handle)
}

//go:wasmimport wasi:filesystem/types@0.3.0 [future-new-1][method]descriptor.read-via-stream
func wasm_future_new_result_unit_error_code() uint64

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][future-read-1][method]descriptor.read-via-stream
func wasm_future_read_result_unit_error_code(handle int32, item unsafe.Pointer) uint32

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][future-write-1][method]descriptor.read-via-stream
func wasm_future_write_result_unit_error_code(handle int32, item unsafe.Pointer) uint32

//go:wasmimport wasi:filesystem/types@0.3.0 [future-drop-readable-1][method]descriptor.read-via-stream
func wasm_future_drop_readable_result_unit_error_code(handle int32)

//go:wasmimport wasi:filesystem/types@0.3.0 [future-drop-writable-1][method]descriptor.read-via-stream
func wasm_future_drop_writable_result_unit_error_code(handle int32)

func wasm_future_lift_result_unit_error_code(src unsafe.Pointer) witTypes.Result[witTypes.Unit, ErrorCode] {
	var result witTypes.Result[witTypes.Unit, ErrorCode]
	switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(src), 0))) {
	case 0:

		result = witTypes.Ok[witTypes.Unit, ErrorCode](witTypes.Unit{})
	case 1:
		var variant ErrorCode
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(src), 4))) {
		case 0:

			variant = MakeErrorCodeAccess()

		case 1:

			variant = MakeErrorCodeAlready()

		case 2:

			variant = MakeErrorCodeBadDescriptor()

		case 3:

			variant = MakeErrorCodeBusy()

		case 4:

			variant = MakeErrorCodeDeadlock()

		case 5:

			variant = MakeErrorCodeQuota()

		case 6:

			variant = MakeErrorCodeExist()

		case 7:

			variant = MakeErrorCodeFileTooLarge()

		case 8:

			variant = MakeErrorCodeIllegalByteSequence()

		case 9:

			variant = MakeErrorCodeInProgress()

		case 10:

			variant = MakeErrorCodeInterrupted()

		case 11:

			variant = MakeErrorCodeInvalid()

		case 12:

			variant = MakeErrorCodeIo()

		case 13:

			variant = MakeErrorCodeIsDirectory()

		case 14:

			variant = MakeErrorCodeLoop()

		case 15:

			variant = MakeErrorCodeTooManyLinks()

		case 16:

			variant = MakeErrorCodeMessageSize()

		case 17:

			variant = MakeErrorCodeNameTooLong()

		case 18:

			variant = MakeErrorCodeNoDevice()

		case 19:

			variant = MakeErrorCodeNoEntry()

		case 20:

			variant = MakeErrorCodeNoLock()

		case 21:

			variant = MakeErrorCodeInsufficientMemory()

		case 22:

			variant = MakeErrorCodeInsufficientSpace()

		case 23:

			variant = MakeErrorCodeNotDirectory()

		case 24:

			variant = MakeErrorCodeNotEmpty()

		case 25:

			variant = MakeErrorCodeNotRecoverable()

		case 26:

			variant = MakeErrorCodeUnsupported()

		case 27:

			variant = MakeErrorCodeNoTty()

		case 28:

			variant = MakeErrorCodeNoSuchDevice()

		case 29:

			variant = MakeErrorCodeOverflow()

		case 30:

			variant = MakeErrorCodeNotPermitted()

		case 31:

			variant = MakeErrorCodePipe()

		case 32:

			variant = MakeErrorCodeReadOnly()

		case 33:

			variant = MakeErrorCodeInvalidSeek()

		case 34:

			variant = MakeErrorCodeTextFileBusy()

		case 35:

			variant = MakeErrorCodeCrossDevice()

		case 36:
			var option witTypes.Option[string]
			switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(src), (2 * 4)))) {
			case 0:

				option = witTypes.None[string]()
			case 1:
				value := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(src), (3 * 4)))))), *(*uint32)(unsafe.Add(unsafe.Pointer(src), (4 * 4))))

				option = witTypes.Some[string](value)
			default:
				panic("unreachable")
			}

			variant = MakeErrorCodeOther(option)

		default:
			panic("unreachable")
		}

		result = witTypes.Err[witTypes.Unit, ErrorCode](variant)
	default:
		panic("unreachable")
	}

	return result
}

func wasm_future_lower_result_unit_error_code(
	pinner *runtime.Pinner,
	value witTypes.Result[witTypes.Unit, ErrorCode],
	dst unsafe.Pointer,
) func() {

	switch value.Tag() {
	case witTypes.ResultOk:

		*(*int8)(unsafe.Add(unsafe.Pointer(dst), 0)) = int8(int32(0))

	case witTypes.ResultErr:
		payload := value.Err()
		*(*int8)(unsafe.Add(unsafe.Pointer(dst), 0)) = int8(int32(1))

		switch payload.Tag() {
		case ErrorCodeAccess:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(0))

		case ErrorCodeAlready:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(1))

		case ErrorCodeBadDescriptor:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(2))

		case ErrorCodeBusy:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(3))

		case ErrorCodeDeadlock:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(4))

		case ErrorCodeQuota:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(5))

		case ErrorCodeExist:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(6))

		case ErrorCodeFileTooLarge:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(7))

		case ErrorCodeIllegalByteSequence:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(8))

		case ErrorCodeInProgress:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(9))

		case ErrorCodeInterrupted:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(10))

		case ErrorCodeInvalid:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(11))

		case ErrorCodeIo:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(12))

		case ErrorCodeIsDirectory:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(13))

		case ErrorCodeLoop:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(14))

		case ErrorCodeTooManyLinks:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(15))

		case ErrorCodeMessageSize:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(16))

		case ErrorCodeNameTooLong:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(17))

		case ErrorCodeNoDevice:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(18))

		case ErrorCodeNoEntry:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(19))

		case ErrorCodeNoLock:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(20))

		case ErrorCodeInsufficientMemory:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(21))

		case ErrorCodeInsufficientSpace:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(22))

		case ErrorCodeNotDirectory:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(23))

		case ErrorCodeNotEmpty:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(24))

		case ErrorCodeNotRecoverable:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(25))

		case ErrorCodeUnsupported:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(26))

		case ErrorCodeNoTty:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(27))

		case ErrorCodeNoSuchDevice:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(28))

		case ErrorCodeOverflow:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(29))

		case ErrorCodeNotPermitted:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(30))

		case ErrorCodePipe:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(31))

		case ErrorCodeReadOnly:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(32))

		case ErrorCodeInvalidSeek:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(33))

		case ErrorCodeTextFileBusy:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(34))

		case ErrorCodeCrossDevice:

			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(35))

		case ErrorCodeOther:
			payload := payload.Other()
			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(36))

			switch payload.Tag() {
			case witTypes.OptionNone:
				*(*int8)(unsafe.Add(unsafe.Pointer(dst), (2 * 4))) = int8(int32(0))

			case witTypes.OptionSome:
				payload := payload.Some()
				*(*int8)(unsafe.Add(unsafe.Pointer(dst), (2 * 4))) = int8(int32(1))
				utf8 := unsafe.Pointer(unsafe.StringData(payload))
				pinner.Pin(utf8)
				*(*uint32)(unsafe.Add(unsafe.Pointer(dst), (4 * 4))) = uint32(uint32(len(payload)))
				*(*uint32)(unsafe.Add(unsafe.Pointer(dst), (3 * 4))) = uint32(uintptr(uintptr(utf8)))

			default:
				panic("unreachable")
			}

		default:
			panic("unreachable")
		}

	default:
		panic("unreachable")
	}

	return func() {}
}

var wasm_future_vtable_result_unit_error_code = witTypes.FutureVtable[witTypes.Result[witTypes.Unit, ErrorCode]]{
	(5 * 4),
	4,
	wasm_future_read_result_unit_error_code,
	wasm_future_write_result_unit_error_code,
	nil,
	nil,
	wasm_future_drop_readable_result_unit_error_code,
	wasm_future_drop_writable_result_unit_error_code,
	wasm_future_lift_result_unit_error_code,
	wasm_future_lower_result_unit_error_code,
}

func MakeFutureResultUnitErrorCode() (*witTypes.FutureWriter[witTypes.Result[witTypes.Unit, ErrorCode]], *witTypes.FutureReader[witTypes.Result[witTypes.Unit, ErrorCode]]) {
	pair := wasm_future_new_result_unit_error_code()
	return witTypes.MakeFutureWriter[witTypes.Result[witTypes.Unit, ErrorCode]](&wasm_future_vtable_result_unit_error_code, int32(pair>>32)),
		witTypes.MakeFutureReader[witTypes.Result[witTypes.Unit, ErrorCode]](&wasm_future_vtable_result_unit_error_code, int32(pair&0xFFFFFFFF))
}

func LiftFutureResultUnitErrorCode(handle int32) *witTypes.FutureReader[witTypes.Result[witTypes.Unit, ErrorCode]] {
	return witTypes.MakeFutureReader[witTypes.Result[witTypes.Unit, ErrorCode]](&wasm_future_vtable_result_unit_error_code, handle)
}

//go:wasmimport wasi:filesystem/types@0.3.0 [stream-new-0][method]descriptor.read-directory
func wasm_stream_new_directory_entry() uint64

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][stream-read-0][method]descriptor.read-directory
func wasm_stream_read_directory_entry(handle int32, item unsafe.Pointer, count uint32) uint32

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][stream-write-0][method]descriptor.read-directory
func wasm_stream_write_directory_entry(handle int32, item unsafe.Pointer, count uint32) uint32

//go:wasmimport wasi:filesystem/types@0.3.0 [stream-drop-readable-0][method]descriptor.read-directory
func wasm_stream_drop_readable_directory_entry(handle int32)

//go:wasmimport wasi:filesystem/types@0.3.0 [stream-drop-writable-0][method]descriptor.read-directory
func wasm_stream_drop_writable_directory_entry(handle int32)

func wasm_stream_lift_directory_entry(src unsafe.Pointer) DirectoryEntry {
	var variant DescriptorType
	switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(src), 0))) {
	case 0:

		variant = MakeDescriptorTypeBlockDevice()

	case 1:

		variant = MakeDescriptorTypeCharacterDevice()

	case 2:

		variant = MakeDescriptorTypeDirectory()

	case 3:

		variant = MakeDescriptorTypeFifo()

	case 4:

		variant = MakeDescriptorTypeSymbolicLink()

	case 5:

		variant = MakeDescriptorTypeRegularFile()

	case 6:

		variant = MakeDescriptorTypeSocket()

	case 7:
		var option witTypes.Option[string]
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(src), 4))) {
		case 0:

			option = witTypes.None[string]()
		case 1:
			value := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(src), (2 * 4)))))), *(*uint32)(unsafe.Add(unsafe.Pointer(src), (3 * 4))))

			option = witTypes.Some[string](value)
		default:
			panic("unreachable")
		}

		variant = MakeDescriptorTypeOther(option)

	default:
		panic("unreachable")
	}
	value0 := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(src), (4 * 4)))))), *(*uint32)(unsafe.Add(unsafe.Pointer(src), (5 * 4))))

	return DirectoryEntry{variant, value0}
}

func wasm_stream_lower_directory_entry(
	pinner *runtime.Pinner,
	value DirectoryEntry,
	dst unsafe.Pointer,
) func() {

	switch (value).Type.Tag() {
	case DescriptorTypeBlockDevice:

		*(*int8)(unsafe.Add(unsafe.Pointer(dst), 0)) = int8(int32(0))

	case DescriptorTypeCharacterDevice:

		*(*int8)(unsafe.Add(unsafe.Pointer(dst), 0)) = int8(int32(1))

	case DescriptorTypeDirectory:

		*(*int8)(unsafe.Add(unsafe.Pointer(dst), 0)) = int8(int32(2))

	case DescriptorTypeFifo:

		*(*int8)(unsafe.Add(unsafe.Pointer(dst), 0)) = int8(int32(3))

	case DescriptorTypeSymbolicLink:

		*(*int8)(unsafe.Add(unsafe.Pointer(dst), 0)) = int8(int32(4))

	case DescriptorTypeRegularFile:

		*(*int8)(unsafe.Add(unsafe.Pointer(dst), 0)) = int8(int32(5))

	case DescriptorTypeSocket:

		*(*int8)(unsafe.Add(unsafe.Pointer(dst), 0)) = int8(int32(6))

	case DescriptorTypeOther:
		payload := (value).Type.Other()
		*(*int8)(unsafe.Add(unsafe.Pointer(dst), 0)) = int8(int32(7))

		switch payload.Tag() {
		case witTypes.OptionNone:
			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(0))

		case witTypes.OptionSome:
			payload := payload.Some()
			*(*int8)(unsafe.Add(unsafe.Pointer(dst), 4)) = int8(int32(1))
			utf8 := unsafe.Pointer(unsafe.StringData(payload))
			pinner.Pin(utf8)
			*(*uint32)(unsafe.Add(unsafe.Pointer(dst), (3 * 4))) = uint32(uint32(len(payload)))
			*(*uint32)(unsafe.Add(unsafe.Pointer(dst), (2 * 4))) = uint32(uintptr(uintptr(utf8)))

		default:
			panic("unreachable")
		}

	default:
		panic("unreachable")
	}
	utf81 := unsafe.Pointer(unsafe.StringData((value).Name))
	pinner.Pin(utf81)
	*(*uint32)(unsafe.Add(unsafe.Pointer(dst), (5 * 4))) = uint32(uint32(len((value).Name)))
	*(*uint32)(unsafe.Add(unsafe.Pointer(dst), (4 * 4))) = uint32(uintptr(uintptr(utf81)))

	return func() {}
}

var wasm_stream_vtable_directory_entry = witTypes.StreamVtable[DirectoryEntry]{
	(6 * 4),
	4,
	wasm_stream_read_directory_entry,
	wasm_stream_write_directory_entry,
	nil,
	nil,
	wasm_stream_drop_readable_directory_entry,
	wasm_stream_drop_writable_directory_entry,
	wasm_stream_lift_directory_entry,
	wasm_stream_lower_directory_entry,
}

func MakeStreamDirectoryEntry() (*witTypes.StreamWriter[DirectoryEntry], *witTypes.StreamReader[DirectoryEntry]) {
	pair := wasm_stream_new_directory_entry()
	return witTypes.MakeStreamWriter[DirectoryEntry](&wasm_stream_vtable_directory_entry, int32(pair>>32)),
		witTypes.MakeStreamReader[DirectoryEntry](&wasm_stream_vtable_directory_entry, int32(pair&0xFFFFFFFF))
}

func LiftStreamDirectoryEntry(handle int32) *witTypes.StreamReader[DirectoryEntry] {
	return witTypes.MakeStreamReader[DirectoryEntry](&wasm_stream_vtable_directory_entry, handle)
}

type Instant = wasi_clocks_system_clock.Instant
type Filesize = uint64

const (
	DescriptorTypeBlockDevice     uint8 = 0
	DescriptorTypeCharacterDevice uint8 = 1
	DescriptorTypeDirectory       uint8 = 2
	DescriptorTypeFifo            uint8 = 3
	DescriptorTypeSymbolicLink    uint8 = 4
	DescriptorTypeRegularFile     uint8 = 5
	DescriptorTypeSocket          uint8 = 6
	DescriptorTypeOther           uint8 = 7
)

type DescriptorType struct {
	tag   uint8
	value any
}

func (self DescriptorType) Tag() uint8 {
	return self.tag
}

func (self DescriptorType) Other() witTypes.Option[string] {
	if self.tag != DescriptorTypeOther {
		panic("tag mismatch")
	}
	return self.value.(witTypes.Option[string])
}

func MakeDescriptorTypeBlockDevice() DescriptorType {
	return DescriptorType{DescriptorTypeBlockDevice, nil}
}
func MakeDescriptorTypeCharacterDevice() DescriptorType {
	return DescriptorType{DescriptorTypeCharacterDevice, nil}
}
func MakeDescriptorTypeDirectory() DescriptorType {
	return DescriptorType{DescriptorTypeDirectory, nil}
}
func MakeDescriptorTypeFifo() DescriptorType {
	return DescriptorType{DescriptorTypeFifo, nil}
}
func MakeDescriptorTypeSymbolicLink() DescriptorType {
	return DescriptorType{DescriptorTypeSymbolicLink, nil}
}
func MakeDescriptorTypeRegularFile() DescriptorType {
	return DescriptorType{DescriptorTypeRegularFile, nil}
}
func MakeDescriptorTypeSocket() DescriptorType {
	return DescriptorType{DescriptorTypeSocket, nil}
}
func MakeDescriptorTypeOther(value witTypes.Option[string]) DescriptorType {
	return DescriptorType{DescriptorTypeOther, value}
}

const (
	DescriptorFlagsRead               uint8 = 1 << 0
	DescriptorFlagsWrite              uint8 = 1 << 1
	DescriptorFlagsFileIntegritySync  uint8 = 1 << 2
	DescriptorFlagsDataIntegritySync  uint8 = 1 << 3
	DescriptorFlagsRequestedWriteSync uint8 = 1 << 4
	DescriptorFlagsMutateDirectory    uint8 = 1 << 5
)

type DescriptorFlags = uint8

const (
	PathFlagsSymlinkFollow uint8 = 1 << 0
)

type PathFlags = uint8

const (
	OpenFlagsCreate    uint8 = 1 << 0
	OpenFlagsDirectory uint8 = 1 << 1
	OpenFlagsExclusive uint8 = 1 << 2
	OpenFlagsTruncate  uint8 = 1 << 3
)

type OpenFlags = uint8
type LinkCount = uint64

type DescriptorStat struct {
	Type                      DescriptorType
	LinkCount                 uint64
	Size                      uint64
	DataAccessTimestamp       witTypes.Option[wasi_clocks_system_clock.Instant]
	DataModificationTimestamp witTypes.Option[wasi_clocks_system_clock.Instant]
	StatusChangeTimestamp     witTypes.Option[wasi_clocks_system_clock.Instant]
}

const (
	NewTimestampNoChange  uint8 = 0
	NewTimestampNow       uint8 = 1
	NewTimestampTimestamp uint8 = 2
)

type NewTimestamp struct {
	tag   uint8
	value any
}

func (self NewTimestamp) Tag() uint8 {
	return self.tag
}

func (self NewTimestamp) Timestamp() wasi_clocks_system_clock.Instant {
	if self.tag != NewTimestampTimestamp {
		panic("tag mismatch")
	}
	return self.value.(wasi_clocks_system_clock.Instant)
}

func MakeNewTimestampNoChange() NewTimestamp {
	return NewTimestamp{NewTimestampNoChange, nil}
}
func MakeNewTimestampNow() NewTimestamp {
	return NewTimestamp{NewTimestampNow, nil}
}
func MakeNewTimestampTimestamp(value wasi_clocks_system_clock.Instant) NewTimestamp {
	return NewTimestamp{NewTimestampTimestamp, value}
}

type DirectoryEntry struct {
	Type DescriptorType
	Name string
}

const (
	ErrorCodeAccess              uint8 = 0
	ErrorCodeAlready             uint8 = 1
	ErrorCodeBadDescriptor       uint8 = 2
	ErrorCodeBusy                uint8 = 3
	ErrorCodeDeadlock            uint8 = 4
	ErrorCodeQuota               uint8 = 5
	ErrorCodeExist               uint8 = 6
	ErrorCodeFileTooLarge        uint8 = 7
	ErrorCodeIllegalByteSequence uint8 = 8
	ErrorCodeInProgress          uint8 = 9
	ErrorCodeInterrupted         uint8 = 10
	ErrorCodeInvalid             uint8 = 11
	ErrorCodeIo                  uint8 = 12
	ErrorCodeIsDirectory         uint8 = 13
	ErrorCodeLoop                uint8 = 14
	ErrorCodeTooManyLinks        uint8 = 15
	ErrorCodeMessageSize         uint8 = 16
	ErrorCodeNameTooLong         uint8 = 17
	ErrorCodeNoDevice            uint8 = 18
	ErrorCodeNoEntry             uint8 = 19
	ErrorCodeNoLock              uint8 = 20
	ErrorCodeInsufficientMemory  uint8 = 21
	ErrorCodeInsufficientSpace   uint8 = 22
	ErrorCodeNotDirectory        uint8 = 23
	ErrorCodeNotEmpty            uint8 = 24
	ErrorCodeNotRecoverable      uint8 = 25
	ErrorCodeUnsupported         uint8 = 26
	ErrorCodeNoTty               uint8 = 27
	ErrorCodeNoSuchDevice        uint8 = 28
	ErrorCodeOverflow            uint8 = 29
	ErrorCodeNotPermitted        uint8 = 30
	ErrorCodePipe                uint8 = 31
	ErrorCodeReadOnly            uint8 = 32
	ErrorCodeInvalidSeek         uint8 = 33
	ErrorCodeTextFileBusy        uint8 = 34
	ErrorCodeCrossDevice         uint8 = 35
	ErrorCodeOther               uint8 = 36
)

type ErrorCode struct {
	tag   uint8
	value any
}

func (self ErrorCode) Tag() uint8 {
	return self.tag
}

func (self ErrorCode) Other() witTypes.Option[string] {
	if self.tag != ErrorCodeOther {
		panic("tag mismatch")
	}
	return self.value.(witTypes.Option[string])
}

func MakeErrorCodeAccess() ErrorCode {
	return ErrorCode{ErrorCodeAccess, nil}
}
func MakeErrorCodeAlready() ErrorCode {
	return ErrorCode{ErrorCodeAlready, nil}
}
func MakeErrorCodeBadDescriptor() ErrorCode {
	return ErrorCode{ErrorCodeBadDescriptor, nil}
}
func MakeErrorCodeBusy() ErrorCode {
	return ErrorCode{ErrorCodeBusy, nil}
}
func MakeErrorCodeDeadlock() ErrorCode {
	return ErrorCode{ErrorCodeDeadlock, nil}
}
func MakeErrorCodeQuota() ErrorCode {
	return ErrorCode{ErrorCodeQuota, nil}
}
func MakeErrorCodeExist() ErrorCode {
	return ErrorCode{ErrorCodeExist, nil}
}
func MakeErrorCodeFileTooLarge() ErrorCode {
	return ErrorCode{ErrorCodeFileTooLarge, nil}
}
func MakeErrorCodeIllegalByteSequence() ErrorCode {
	return ErrorCode{ErrorCodeIllegalByteSequence, nil}
}
func MakeErrorCodeInProgress() ErrorCode {
	return ErrorCode{ErrorCodeInProgress, nil}
}
func MakeErrorCodeInterrupted() ErrorCode {
	return ErrorCode{ErrorCodeInterrupted, nil}
}
func MakeErrorCodeInvalid() ErrorCode {
	return ErrorCode{ErrorCodeInvalid, nil}
}
func MakeErrorCodeIo() ErrorCode {
	return ErrorCode{ErrorCodeIo, nil}
}
func MakeErrorCodeIsDirectory() ErrorCode {
	return ErrorCode{ErrorCodeIsDirectory, nil}
}
func MakeErrorCodeLoop() ErrorCode {
	return ErrorCode{ErrorCodeLoop, nil}
}
func MakeErrorCodeTooManyLinks() ErrorCode {
	return ErrorCode{ErrorCodeTooManyLinks, nil}
}
func MakeErrorCodeMessageSize() ErrorCode {
	return ErrorCode{ErrorCodeMessageSize, nil}
}
func MakeErrorCodeNameTooLong() ErrorCode {
	return ErrorCode{ErrorCodeNameTooLong, nil}
}
func MakeErrorCodeNoDevice() ErrorCode {
	return ErrorCode{ErrorCodeNoDevice, nil}
}
func MakeErrorCodeNoEntry() ErrorCode {
	return ErrorCode{ErrorCodeNoEntry, nil}
}
func MakeErrorCodeNoLock() ErrorCode {
	return ErrorCode{ErrorCodeNoLock, nil}
}
func MakeErrorCodeInsufficientMemory() ErrorCode {
	return ErrorCode{ErrorCodeInsufficientMemory, nil}
}
func MakeErrorCodeInsufficientSpace() ErrorCode {
	return ErrorCode{ErrorCodeInsufficientSpace, nil}
}
func MakeErrorCodeNotDirectory() ErrorCode {
	return ErrorCode{ErrorCodeNotDirectory, nil}
}
func MakeErrorCodeNotEmpty() ErrorCode {
	return ErrorCode{ErrorCodeNotEmpty, nil}
}
func MakeErrorCodeNotRecoverable() ErrorCode {
	return ErrorCode{ErrorCodeNotRecoverable, nil}
}
func MakeErrorCodeUnsupported() ErrorCode {
	return ErrorCode{ErrorCodeUnsupported, nil}
}
func MakeErrorCodeNoTty() ErrorCode {
	return ErrorCode{ErrorCodeNoTty, nil}
}
func MakeErrorCodeNoSuchDevice() ErrorCode {
	return ErrorCode{ErrorCodeNoSuchDevice, nil}
}
func MakeErrorCodeOverflow() ErrorCode {
	return ErrorCode{ErrorCodeOverflow, nil}
}
func MakeErrorCodeNotPermitted() ErrorCode {
	return ErrorCode{ErrorCodeNotPermitted, nil}
}
func MakeErrorCodePipe() ErrorCode {
	return ErrorCode{ErrorCodePipe, nil}
}
func MakeErrorCodeReadOnly() ErrorCode {
	return ErrorCode{ErrorCodeReadOnly, nil}
}
func MakeErrorCodeInvalidSeek() ErrorCode {
	return ErrorCode{ErrorCodeInvalidSeek, nil}
}
func MakeErrorCodeTextFileBusy() ErrorCode {
	return ErrorCode{ErrorCodeTextFileBusy, nil}
}
func MakeErrorCodeCrossDevice() ErrorCode {
	return ErrorCode{ErrorCodeCrossDevice, nil}
}
func MakeErrorCodeOther(value witTypes.Option[string]) ErrorCode {
	return ErrorCode{ErrorCodeOther, value}
}

const (
	AdviceNormal     uint8 = 0
	AdviceSequential uint8 = 1
	AdviceRandom     uint8 = 2
	AdviceWillNeed   uint8 = 3
	AdviceDontNeed   uint8 = 4
	AdviceNoReuse    uint8 = 5
)

type Advice = uint8

type MetadataHashValue struct {
	Lower uint64
	Upper uint64
}

//go:wasmimport wasi:filesystem/types@0.3.0 [resource-drop]descriptor
func resourceDropDescriptor(handle int32)

type Descriptor struct {
	handle *witRuntime.Handle
}

func (self *Descriptor) TakeHandle() int32 {
	return self.handle.Take()
}

func (self *Descriptor) SetHandle(handle int32) {
	self.handle.Set(handle)
}

func (self *Descriptor) Handle() int32 {
	return self.handle.Use()
}

func (self *Descriptor) Drop() {
	handle := self.handle.TakeOrNil()
	if handle != 0 {
		resourceDropDescriptor(handle)
	}
}

func DescriptorFromOwnHandle(handleValue int32) *Descriptor {
	handle := witRuntime.MakeHandle(handleValue)
	value := &Descriptor{handle}
	runtime.AddCleanup(value, func(_ int) {
		handleValue := handle.TakeOrNil()
		if handleValue != 0 {
			resourceDropDescriptor(handleValue)
		}
	}, 0)
	return value
}

func DescriptorFromBorrowHandle(handleValue int32) *Descriptor {
	handle := witRuntime.MakeHandle(handleValue)
	return &Descriptor{handle}
}

//go:wasmimport wasi:filesystem/types@0.3.0 [method]descriptor.read-via-stream
func wasm_import_method_descriptor_read_via_stream(arg0 int32, arg1 int64, arg2 uintptr)

func (self *Descriptor) ReadViaStream(offset uint64) (*witTypes.StreamReader[uint8], *witTypes.FutureReader[witTypes.Result[witTypes.Unit, ErrorCode]]) {
	pinner := &runtime.Pinner{}
	defer pinner.Unpin()

	returnArea := uintptr(witRuntime.Allocate(pinner, 8, 4))
	wasm_import_method_descriptor_read_via_stream((self).Handle(), int64(offset), returnArea)
	result := witTypes.Tuple2[*witTypes.StreamReader[uint8], *witTypes.FutureReader[witTypes.Result[witTypes.Unit, ErrorCode]]]{LiftStreamU8(*(*int32)(unsafe.Add(unsafe.Pointer(returnArea), 0))), LiftFutureResultUnitErrorCode(*(*int32)(unsafe.Add(unsafe.Pointer(returnArea), 4)))}
	tuple := result
	return tuple.F0, tuple.F1

}

//go:wasmimport wasi:filesystem/types@0.3.0 [method]descriptor.write-via-stream
func wasm_import_method_descriptor_write_via_stream(arg0 int32, arg1 int32, arg2 int64) int32

func (self *Descriptor) WriteViaStream(data *witTypes.StreamReader[uint8], offset uint64) *witTypes.FutureReader[witTypes.Result[witTypes.Unit, ErrorCode]] {

	result := wasm_import_method_descriptor_write_via_stream((self).Handle(), (data).TakeHandle(), int64(offset))
	return LiftFutureResultUnitErrorCode(result)

}

//go:wasmimport wasi:filesystem/types@0.3.0 [method]descriptor.append-via-stream
func wasm_import_method_descriptor_append_via_stream(arg0 int32, arg1 int32) int32

func (self *Descriptor) AppendViaStream(data *witTypes.StreamReader[uint8]) *witTypes.FutureReader[witTypes.Result[witTypes.Unit, ErrorCode]] {

	result := wasm_import_method_descriptor_append_via_stream((self).Handle(), (data).TakeHandle())
	return LiftFutureResultUnitErrorCode(result)

}

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][method]descriptor.advise
func wasm_import_method_descriptor_advise(arg0 int32, arg1 int64, arg2 int64, arg3 int32, arg4 uintptr) int32

func (self *Descriptor) Advise(offset uint64, length uint64, advice Advice) witTypes.Result[witTypes.Unit, ErrorCode] {
	pinner := &runtime.Pinner{}
	defer pinner.Unpin()

	returnArea := uintptr(witRuntime.Allocate(pinner, (5 * 4), 4))

	witAsync.SubtaskWait(uint32(wasm_import_method_descriptor_advise((self).Handle(), int64(offset), int64(length), int32(advice), returnArea)))
	var result witTypes.Result[witTypes.Unit, ErrorCode]
	switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 0))) {
	case 0:

		result = witTypes.Ok[witTypes.Unit, ErrorCode](witTypes.Unit{})
	case 1:
		var variant ErrorCode
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 4))) {
		case 0:

			variant = MakeErrorCodeAccess()

		case 1:

			variant = MakeErrorCodeAlready()

		case 2:

			variant = MakeErrorCodeBadDescriptor()

		case 3:

			variant = MakeErrorCodeBusy()

		case 4:

			variant = MakeErrorCodeDeadlock()

		case 5:

			variant = MakeErrorCodeQuota()

		case 6:

			variant = MakeErrorCodeExist()

		case 7:

			variant = MakeErrorCodeFileTooLarge()

		case 8:

			variant = MakeErrorCodeIllegalByteSequence()

		case 9:

			variant = MakeErrorCodeInProgress()

		case 10:

			variant = MakeErrorCodeInterrupted()

		case 11:

			variant = MakeErrorCodeInvalid()

		case 12:

			variant = MakeErrorCodeIo()

		case 13:

			variant = MakeErrorCodeIsDirectory()

		case 14:

			variant = MakeErrorCodeLoop()

		case 15:

			variant = MakeErrorCodeTooManyLinks()

		case 16:

			variant = MakeErrorCodeMessageSize()

		case 17:

			variant = MakeErrorCodeNameTooLong()

		case 18:

			variant = MakeErrorCodeNoDevice()

		case 19:

			variant = MakeErrorCodeNoEntry()

		case 20:

			variant = MakeErrorCodeNoLock()

		case 21:

			variant = MakeErrorCodeInsufficientMemory()

		case 22:

			variant = MakeErrorCodeInsufficientSpace()

		case 23:

			variant = MakeErrorCodeNotDirectory()

		case 24:

			variant = MakeErrorCodeNotEmpty()

		case 25:

			variant = MakeErrorCodeNotRecoverable()

		case 26:

			variant = MakeErrorCodeUnsupported()

		case 27:

			variant = MakeErrorCodeNoTty()

		case 28:

			variant = MakeErrorCodeNoSuchDevice()

		case 29:

			variant = MakeErrorCodeOverflow()

		case 30:

			variant = MakeErrorCodeNotPermitted()

		case 31:

			variant = MakeErrorCodePipe()

		case 32:

			variant = MakeErrorCodeReadOnly()

		case 33:

			variant = MakeErrorCodeInvalidSeek()

		case 34:

			variant = MakeErrorCodeTextFileBusy()

		case 35:

			variant = MakeErrorCodeCrossDevice()

		case 36:
			var option witTypes.Option[string]
			switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (2 * 4)))) {
			case 0:

				option = witTypes.None[string]()
			case 1:
				value := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (3 * 4)))))), *(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (4 * 4))))

				option = witTypes.Some[string](value)
			default:
				panic("unreachable")
			}

			variant = MakeErrorCodeOther(option)

		default:
			panic("unreachable")
		}

		result = witTypes.Err[witTypes.Unit, ErrorCode](variant)
	default:
		panic("unreachable")
	}

	return result

}

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][method]descriptor.sync-data
func wasm_import_method_descriptor_sync_data(arg0 int32, arg1 uintptr) int32

func (self *Descriptor) SyncData() witTypes.Result[witTypes.Unit, ErrorCode] {
	pinner := &runtime.Pinner{}
	defer pinner.Unpin()

	returnArea := uintptr(witRuntime.Allocate(pinner, (5 * 4), 4))

	witAsync.SubtaskWait(uint32(wasm_import_method_descriptor_sync_data((self).Handle(), returnArea)))
	var result witTypes.Result[witTypes.Unit, ErrorCode]
	switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 0))) {
	case 0:

		result = witTypes.Ok[witTypes.Unit, ErrorCode](witTypes.Unit{})
	case 1:
		var variant ErrorCode
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 4))) {
		case 0:

			variant = MakeErrorCodeAccess()

		case 1:

			variant = MakeErrorCodeAlready()

		case 2:

			variant = MakeErrorCodeBadDescriptor()

		case 3:

			variant = MakeErrorCodeBusy()

		case 4:

			variant = MakeErrorCodeDeadlock()

		case 5:

			variant = MakeErrorCodeQuota()

		case 6:

			variant = MakeErrorCodeExist()

		case 7:

			variant = MakeErrorCodeFileTooLarge()

		case 8:

			variant = MakeErrorCodeIllegalByteSequence()

		case 9:

			variant = MakeErrorCodeInProgress()

		case 10:

			variant = MakeErrorCodeInterrupted()

		case 11:

			variant = MakeErrorCodeInvalid()

		case 12:

			variant = MakeErrorCodeIo()

		case 13:

			variant = MakeErrorCodeIsDirectory()

		case 14:

			variant = MakeErrorCodeLoop()

		case 15:

			variant = MakeErrorCodeTooManyLinks()

		case 16:

			variant = MakeErrorCodeMessageSize()

		case 17:

			variant = MakeErrorCodeNameTooLong()

		case 18:

			variant = MakeErrorCodeNoDevice()

		case 19:

			variant = MakeErrorCodeNoEntry()

		case 20:

			variant = MakeErrorCodeNoLock()

		case 21:

			variant = MakeErrorCodeInsufficientMemory()

		case 22:

			variant = MakeErrorCodeInsufficientSpace()

		case 23:

			variant = MakeErrorCodeNotDirectory()

		case 24:

			variant = MakeErrorCodeNotEmpty()

		case 25:

			variant = MakeErrorCodeNotRecoverable()

		case 26:

			variant = MakeErrorCodeUnsupported()

		case 27:

			variant = MakeErrorCodeNoTty()

		case 28:

			variant = MakeErrorCodeNoSuchDevice()

		case 29:

			variant = MakeErrorCodeOverflow()

		case 30:

			variant = MakeErrorCodeNotPermitted()

		case 31:

			variant = MakeErrorCodePipe()

		case 32:

			variant = MakeErrorCodeReadOnly()

		case 33:

			variant = MakeErrorCodeInvalidSeek()

		case 34:

			variant = MakeErrorCodeTextFileBusy()

		case 35:

			variant = MakeErrorCodeCrossDevice()

		case 36:
			var option witTypes.Option[string]
			switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (2 * 4)))) {
			case 0:

				option = witTypes.None[string]()
			case 1:
				value := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (3 * 4)))))), *(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (4 * 4))))

				option = witTypes.Some[string](value)
			default:
				panic("unreachable")
			}

			variant = MakeErrorCodeOther(option)

		default:
			panic("unreachable")
		}

		result = witTypes.Err[witTypes.Unit, ErrorCode](variant)
	default:
		panic("unreachable")
	}

	return result

}

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][method]descriptor.get-flags
func wasm_import_method_descriptor_get_flags(arg0 int32, arg1 uintptr) int32

func (self *Descriptor) GetFlags() witTypes.Result[DescriptorFlags, ErrorCode] {
	pinner := &runtime.Pinner{}
	defer pinner.Unpin()

	returnArea := uintptr(witRuntime.Allocate(pinner, (5 * 4), 4))

	witAsync.SubtaskWait(uint32(wasm_import_method_descriptor_get_flags((self).Handle(), returnArea)))
	var result witTypes.Result[DescriptorFlags, ErrorCode]
	switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 0))) {
	case 0:

		result = witTypes.Ok[DescriptorFlags, ErrorCode](uint8(uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 4)))))
	case 1:
		var variant ErrorCode
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 4))) {
		case 0:

			variant = MakeErrorCodeAccess()

		case 1:

			variant = MakeErrorCodeAlready()

		case 2:

			variant = MakeErrorCodeBadDescriptor()

		case 3:

			variant = MakeErrorCodeBusy()

		case 4:

			variant = MakeErrorCodeDeadlock()

		case 5:

			variant = MakeErrorCodeQuota()

		case 6:

			variant = MakeErrorCodeExist()

		case 7:

			variant = MakeErrorCodeFileTooLarge()

		case 8:

			variant = MakeErrorCodeIllegalByteSequence()

		case 9:

			variant = MakeErrorCodeInProgress()

		case 10:

			variant = MakeErrorCodeInterrupted()

		case 11:

			variant = MakeErrorCodeInvalid()

		case 12:

			variant = MakeErrorCodeIo()

		case 13:

			variant = MakeErrorCodeIsDirectory()

		case 14:

			variant = MakeErrorCodeLoop()

		case 15:

			variant = MakeErrorCodeTooManyLinks()

		case 16:

			variant = MakeErrorCodeMessageSize()

		case 17:

			variant = MakeErrorCodeNameTooLong()

		case 18:

			variant = MakeErrorCodeNoDevice()

		case 19:

			variant = MakeErrorCodeNoEntry()

		case 20:

			variant = MakeErrorCodeNoLock()

		case 21:

			variant = MakeErrorCodeInsufficientMemory()

		case 22:

			variant = MakeErrorCodeInsufficientSpace()

		case 23:

			variant = MakeErrorCodeNotDirectory()

		case 24:

			variant = MakeErrorCodeNotEmpty()

		case 25:

			variant = MakeErrorCodeNotRecoverable()

		case 26:

			variant = MakeErrorCodeUnsupported()

		case 27:

			variant = MakeErrorCodeNoTty()

		case 28:

			variant = MakeErrorCodeNoSuchDevice()

		case 29:

			variant = MakeErrorCodeOverflow()

		case 30:

			variant = MakeErrorCodeNotPermitted()

		case 31:

			variant = MakeErrorCodePipe()

		case 32:

			variant = MakeErrorCodeReadOnly()

		case 33:

			variant = MakeErrorCodeInvalidSeek()

		case 34:

			variant = MakeErrorCodeTextFileBusy()

		case 35:

			variant = MakeErrorCodeCrossDevice()

		case 36:
			var option witTypes.Option[string]
			switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (2 * 4)))) {
			case 0:

				option = witTypes.None[string]()
			case 1:
				value := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (3 * 4)))))), *(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (4 * 4))))

				option = witTypes.Some[string](value)
			default:
				panic("unreachable")
			}

			variant = MakeErrorCodeOther(option)

		default:
			panic("unreachable")
		}

		result = witTypes.Err[DescriptorFlags, ErrorCode](variant)
	default:
		panic("unreachable")
	}

	return result

}

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][method]descriptor.get-type
func wasm_import_method_descriptor_get_type(arg0 int32, arg1 uintptr) int32

func (self *Descriptor) GetType() witTypes.Result[DescriptorType, ErrorCode] {
	pinner := &runtime.Pinner{}
	defer pinner.Unpin()

	returnArea := uintptr(witRuntime.Allocate(pinner, (5 * 4), 4))

	witAsync.SubtaskWait(uint32(wasm_import_method_descriptor_get_type((self).Handle(), returnArea)))
	var result witTypes.Result[DescriptorType, ErrorCode]
	switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 0))) {
	case 0:
		var variant DescriptorType
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 4))) {
		case 0:

			variant = MakeDescriptorTypeBlockDevice()

		case 1:

			variant = MakeDescriptorTypeCharacterDevice()

		case 2:

			variant = MakeDescriptorTypeDirectory()

		case 3:

			variant = MakeDescriptorTypeFifo()

		case 4:

			variant = MakeDescriptorTypeSymbolicLink()

		case 5:

			variant = MakeDescriptorTypeRegularFile()

		case 6:

			variant = MakeDescriptorTypeSocket()

		case 7:
			var option witTypes.Option[string]
			switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (2 * 4)))) {
			case 0:

				option = witTypes.None[string]()
			case 1:
				value := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (3 * 4)))))), *(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (4 * 4))))

				option = witTypes.Some[string](value)
			default:
				panic("unreachable")
			}

			variant = MakeDescriptorTypeOther(option)

		default:
			panic("unreachable")
		}

		result = witTypes.Ok[DescriptorType, ErrorCode](variant)
	case 1:
		var variant2 ErrorCode
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 4))) {
		case 0:

			variant2 = MakeErrorCodeAccess()

		case 1:

			variant2 = MakeErrorCodeAlready()

		case 2:

			variant2 = MakeErrorCodeBadDescriptor()

		case 3:

			variant2 = MakeErrorCodeBusy()

		case 4:

			variant2 = MakeErrorCodeDeadlock()

		case 5:

			variant2 = MakeErrorCodeQuota()

		case 6:

			variant2 = MakeErrorCodeExist()

		case 7:

			variant2 = MakeErrorCodeFileTooLarge()

		case 8:

			variant2 = MakeErrorCodeIllegalByteSequence()

		case 9:

			variant2 = MakeErrorCodeInProgress()

		case 10:

			variant2 = MakeErrorCodeInterrupted()

		case 11:

			variant2 = MakeErrorCodeInvalid()

		case 12:

			variant2 = MakeErrorCodeIo()

		case 13:

			variant2 = MakeErrorCodeIsDirectory()

		case 14:

			variant2 = MakeErrorCodeLoop()

		case 15:

			variant2 = MakeErrorCodeTooManyLinks()

		case 16:

			variant2 = MakeErrorCodeMessageSize()

		case 17:

			variant2 = MakeErrorCodeNameTooLong()

		case 18:

			variant2 = MakeErrorCodeNoDevice()

		case 19:

			variant2 = MakeErrorCodeNoEntry()

		case 20:

			variant2 = MakeErrorCodeNoLock()

		case 21:

			variant2 = MakeErrorCodeInsufficientMemory()

		case 22:

			variant2 = MakeErrorCodeInsufficientSpace()

		case 23:

			variant2 = MakeErrorCodeNotDirectory()

		case 24:

			variant2 = MakeErrorCodeNotEmpty()

		case 25:

			variant2 = MakeErrorCodeNotRecoverable()

		case 26:

			variant2 = MakeErrorCodeUnsupported()

		case 27:

			variant2 = MakeErrorCodeNoTty()

		case 28:

			variant2 = MakeErrorCodeNoSuchDevice()

		case 29:

			variant2 = MakeErrorCodeOverflow()

		case 30:

			variant2 = MakeErrorCodeNotPermitted()

		case 31:

			variant2 = MakeErrorCodePipe()

		case 32:

			variant2 = MakeErrorCodeReadOnly()

		case 33:

			variant2 = MakeErrorCodeInvalidSeek()

		case 34:

			variant2 = MakeErrorCodeTextFileBusy()

		case 35:

			variant2 = MakeErrorCodeCrossDevice()

		case 36:
			var option1 witTypes.Option[string]
			switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (2 * 4)))) {
			case 0:

				option1 = witTypes.None[string]()
			case 1:
				value0 := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (3 * 4)))))), *(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (4 * 4))))

				option1 = witTypes.Some[string](value0)
			default:
				panic("unreachable")
			}

			variant2 = MakeErrorCodeOther(option1)

		default:
			panic("unreachable")
		}

		result = witTypes.Err[DescriptorType, ErrorCode](variant2)
	default:
		panic("unreachable")
	}

	return result

}

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][method]descriptor.set-size
func wasm_import_method_descriptor_set_size(arg0 int32, arg1 int64, arg2 uintptr) int32

func (self *Descriptor) SetSize(size uint64) witTypes.Result[witTypes.Unit, ErrorCode] {
	pinner := &runtime.Pinner{}
	defer pinner.Unpin()

	returnArea := uintptr(witRuntime.Allocate(pinner, (5 * 4), 4))

	witAsync.SubtaskWait(uint32(wasm_import_method_descriptor_set_size((self).Handle(), int64(size), returnArea)))
	var result witTypes.Result[witTypes.Unit, ErrorCode]
	switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 0))) {
	case 0:

		result = witTypes.Ok[witTypes.Unit, ErrorCode](witTypes.Unit{})
	case 1:
		var variant ErrorCode
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 4))) {
		case 0:

			variant = MakeErrorCodeAccess()

		case 1:

			variant = MakeErrorCodeAlready()

		case 2:

			variant = MakeErrorCodeBadDescriptor()

		case 3:

			variant = MakeErrorCodeBusy()

		case 4:

			variant = MakeErrorCodeDeadlock()

		case 5:

			variant = MakeErrorCodeQuota()

		case 6:

			variant = MakeErrorCodeExist()

		case 7:

			variant = MakeErrorCodeFileTooLarge()

		case 8:

			variant = MakeErrorCodeIllegalByteSequence()

		case 9:

			variant = MakeErrorCodeInProgress()

		case 10:

			variant = MakeErrorCodeInterrupted()

		case 11:

			variant = MakeErrorCodeInvalid()

		case 12:

			variant = MakeErrorCodeIo()

		case 13:

			variant = MakeErrorCodeIsDirectory()

		case 14:

			variant = MakeErrorCodeLoop()

		case 15:

			variant = MakeErrorCodeTooManyLinks()

		case 16:

			variant = MakeErrorCodeMessageSize()

		case 17:

			variant = MakeErrorCodeNameTooLong()

		case 18:

			variant = MakeErrorCodeNoDevice()

		case 19:

			variant = MakeErrorCodeNoEntry()

		case 20:

			variant = MakeErrorCodeNoLock()

		case 21:

			variant = MakeErrorCodeInsufficientMemory()

		case 22:

			variant = MakeErrorCodeInsufficientSpace()

		case 23:

			variant = MakeErrorCodeNotDirectory()

		case 24:

			variant = MakeErrorCodeNotEmpty()

		case 25:

			variant = MakeErrorCodeNotRecoverable()

		case 26:

			variant = MakeErrorCodeUnsupported()

		case 27:

			variant = MakeErrorCodeNoTty()

		case 28:

			variant = MakeErrorCodeNoSuchDevice()

		case 29:

			variant = MakeErrorCodeOverflow()

		case 30:

			variant = MakeErrorCodeNotPermitted()

		case 31:

			variant = MakeErrorCodePipe()

		case 32:

			variant = MakeErrorCodeReadOnly()

		case 33:

			variant = MakeErrorCodeInvalidSeek()

		case 34:

			variant = MakeErrorCodeTextFileBusy()

		case 35:

			variant = MakeErrorCodeCrossDevice()

		case 36:
			var option witTypes.Option[string]
			switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (2 * 4)))) {
			case 0:

				option = witTypes.None[string]()
			case 1:
				value := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (3 * 4)))))), *(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (4 * 4))))

				option = witTypes.Some[string](value)
			default:
				panic("unreachable")
			}

			variant = MakeErrorCodeOther(option)

		default:
			panic("unreachable")
		}

		result = witTypes.Err[witTypes.Unit, ErrorCode](variant)
	default:
		panic("unreachable")
	}

	return result

}

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][method]descriptor.set-times
func wasm_import_method_descriptor_set_times(arg0 uintptr, arg1 uintptr) int32

func (self *Descriptor) SetTimes(dataAccessTimestamp NewTimestamp, dataModificationTimestamp NewTimestamp) witTypes.Result[witTypes.Unit, ErrorCode] {
	pinner := &runtime.Pinner{}
	defer pinner.Unpin()

	returnArea := uintptr(witRuntime.Allocate(pinner, (5 * 4), 4))
	params := witRuntime.Allocate(pinner, 56, 8)
	*(*int32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 0)), 0)) = (self).Handle()

	switch dataAccessTimestamp.Tag() {
	case NewTimestampNoChange:

		*(*int8)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 8)), 0)) = int8(int32(0))

	case NewTimestampNow:

		*(*int8)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 8)), 0)) = int8(int32(1))

	case NewTimestampTimestamp:
		payload := dataAccessTimestamp.Timestamp()
		*(*int8)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 8)), 0)) = int8(int32(2))
		*(*int64)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 8)), 8)) = (payload).Seconds
		*(*int32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 8)), 16)) = int32((payload).Nanoseconds)

	default:
		panic("unreachable")
	}

	switch dataModificationTimestamp.Tag() {
	case NewTimestampNoChange:

		*(*int8)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 32)), 0)) = int8(int32(0))

	case NewTimestampNow:

		*(*int8)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 32)), 0)) = int8(int32(1))

	case NewTimestampTimestamp:
		payload := dataModificationTimestamp.Timestamp()
		*(*int8)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 32)), 0)) = int8(int32(2))
		*(*int64)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 32)), 8)) = (payload).Seconds
		*(*int32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 32)), 16)) = int32((payload).Nanoseconds)

	default:
		panic("unreachable")
	}

	witAsync.SubtaskWait(uint32(wasm_import_method_descriptor_set_times(uintptr(params), returnArea)))
	var result witTypes.Result[witTypes.Unit, ErrorCode]
	switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 0))) {
	case 0:

		result = witTypes.Ok[witTypes.Unit, ErrorCode](witTypes.Unit{})
	case 1:
		var variant ErrorCode
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 4))) {
		case 0:

			variant = MakeErrorCodeAccess()

		case 1:

			variant = MakeErrorCodeAlready()

		case 2:

			variant = MakeErrorCodeBadDescriptor()

		case 3:

			variant = MakeErrorCodeBusy()

		case 4:

			variant = MakeErrorCodeDeadlock()

		case 5:

			variant = MakeErrorCodeQuota()

		case 6:

			variant = MakeErrorCodeExist()

		case 7:

			variant = MakeErrorCodeFileTooLarge()

		case 8:

			variant = MakeErrorCodeIllegalByteSequence()

		case 9:

			variant = MakeErrorCodeInProgress()

		case 10:

			variant = MakeErrorCodeInterrupted()

		case 11:

			variant = MakeErrorCodeInvalid()

		case 12:

			variant = MakeErrorCodeIo()

		case 13:

			variant = MakeErrorCodeIsDirectory()

		case 14:

			variant = MakeErrorCodeLoop()

		case 15:

			variant = MakeErrorCodeTooManyLinks()

		case 16:

			variant = MakeErrorCodeMessageSize()

		case 17:

			variant = MakeErrorCodeNameTooLong()

		case 18:

			variant = MakeErrorCodeNoDevice()

		case 19:

			variant = MakeErrorCodeNoEntry()

		case 20:

			variant = MakeErrorCodeNoLock()

		case 21:

			variant = MakeErrorCodeInsufficientMemory()

		case 22:

			variant = MakeErrorCodeInsufficientSpace()

		case 23:

			variant = MakeErrorCodeNotDirectory()

		case 24:

			variant = MakeErrorCodeNotEmpty()

		case 25:

			variant = MakeErrorCodeNotRecoverable()

		case 26:

			variant = MakeErrorCodeUnsupported()

		case 27:

			variant = MakeErrorCodeNoTty()

		case 28:

			variant = MakeErrorCodeNoSuchDevice()

		case 29:

			variant = MakeErrorCodeOverflow()

		case 30:

			variant = MakeErrorCodeNotPermitted()

		case 31:

			variant = MakeErrorCodePipe()

		case 32:

			variant = MakeErrorCodeReadOnly()

		case 33:

			variant = MakeErrorCodeInvalidSeek()

		case 34:

			variant = MakeErrorCodeTextFileBusy()

		case 35:

			variant = MakeErrorCodeCrossDevice()

		case 36:
			var option witTypes.Option[string]
			switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (2 * 4)))) {
			case 0:

				option = witTypes.None[string]()
			case 1:
				value := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (3 * 4)))))), *(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (4 * 4))))

				option = witTypes.Some[string](value)
			default:
				panic("unreachable")
			}

			variant = MakeErrorCodeOther(option)

		default:
			panic("unreachable")
		}

		result = witTypes.Err[witTypes.Unit, ErrorCode](variant)
	default:
		panic("unreachable")
	}

	return result

}

//go:wasmimport wasi:filesystem/types@0.3.0 [method]descriptor.read-directory
func wasm_import_method_descriptor_read_directory(arg0 int32, arg1 uintptr)

func (self *Descriptor) ReadDirectory() (*witTypes.StreamReader[DirectoryEntry], *witTypes.FutureReader[witTypes.Result[witTypes.Unit, ErrorCode]]) {
	pinner := &runtime.Pinner{}
	defer pinner.Unpin()

	returnArea := uintptr(witRuntime.Allocate(pinner, 8, 4))
	wasm_import_method_descriptor_read_directory((self).Handle(), returnArea)
	result := witTypes.Tuple2[*witTypes.StreamReader[DirectoryEntry], *witTypes.FutureReader[witTypes.Result[witTypes.Unit, ErrorCode]]]{LiftStreamDirectoryEntry(*(*int32)(unsafe.Add(unsafe.Pointer(returnArea), 0))), LiftFutureResultUnitErrorCode(*(*int32)(unsafe.Add(unsafe.Pointer(returnArea), 4)))}
	tuple := result
	return tuple.F0, tuple.F1

}

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][method]descriptor.sync
func wasm_import_method_descriptor_sync(arg0 int32, arg1 uintptr) int32

func (self *Descriptor) Sync() witTypes.Result[witTypes.Unit, ErrorCode] {
	pinner := &runtime.Pinner{}
	defer pinner.Unpin()

	returnArea := uintptr(witRuntime.Allocate(pinner, (5 * 4), 4))

	witAsync.SubtaskWait(uint32(wasm_import_method_descriptor_sync((self).Handle(), returnArea)))
	var result witTypes.Result[witTypes.Unit, ErrorCode]
	switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 0))) {
	case 0:

		result = witTypes.Ok[witTypes.Unit, ErrorCode](witTypes.Unit{})
	case 1:
		var variant ErrorCode
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 4))) {
		case 0:

			variant = MakeErrorCodeAccess()

		case 1:

			variant = MakeErrorCodeAlready()

		case 2:

			variant = MakeErrorCodeBadDescriptor()

		case 3:

			variant = MakeErrorCodeBusy()

		case 4:

			variant = MakeErrorCodeDeadlock()

		case 5:

			variant = MakeErrorCodeQuota()

		case 6:

			variant = MakeErrorCodeExist()

		case 7:

			variant = MakeErrorCodeFileTooLarge()

		case 8:

			variant = MakeErrorCodeIllegalByteSequence()

		case 9:

			variant = MakeErrorCodeInProgress()

		case 10:

			variant = MakeErrorCodeInterrupted()

		case 11:

			variant = MakeErrorCodeInvalid()

		case 12:

			variant = MakeErrorCodeIo()

		case 13:

			variant = MakeErrorCodeIsDirectory()

		case 14:

			variant = MakeErrorCodeLoop()

		case 15:

			variant = MakeErrorCodeTooManyLinks()

		case 16:

			variant = MakeErrorCodeMessageSize()

		case 17:

			variant = MakeErrorCodeNameTooLong()

		case 18:

			variant = MakeErrorCodeNoDevice()

		case 19:

			variant = MakeErrorCodeNoEntry()

		case 20:

			variant = MakeErrorCodeNoLock()

		case 21:

			variant = MakeErrorCodeInsufficientMemory()

		case 22:

			variant = MakeErrorCodeInsufficientSpace()

		case 23:

			variant = MakeErrorCodeNotDirectory()

		case 24:

			variant = MakeErrorCodeNotEmpty()

		case 25:

			variant = MakeErrorCodeNotRecoverable()

		case 26:

			variant = MakeErrorCodeUnsupported()

		case 27:

			variant = MakeErrorCodeNoTty()

		case 28:

			variant = MakeErrorCodeNoSuchDevice()

		case 29:

			variant = MakeErrorCodeOverflow()

		case 30:

			variant = MakeErrorCodeNotPermitted()

		case 31:

			variant = MakeErrorCodePipe()

		case 32:

			variant = MakeErrorCodeReadOnly()

		case 33:

			variant = MakeErrorCodeInvalidSeek()

		case 34:

			variant = MakeErrorCodeTextFileBusy()

		case 35:

			variant = MakeErrorCodeCrossDevice()

		case 36:
			var option witTypes.Option[string]
			switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (2 * 4)))) {
			case 0:

				option = witTypes.None[string]()
			case 1:
				value := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (3 * 4)))))), *(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (4 * 4))))

				option = witTypes.Some[string](value)
			default:
				panic("unreachable")
			}

			variant = MakeErrorCodeOther(option)

		default:
			panic("unreachable")
		}

		result = witTypes.Err[witTypes.Unit, ErrorCode](variant)
	default:
		panic("unreachable")
	}

	return result

}

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][method]descriptor.create-directory-at
func wasm_import_method_descriptor_create_directory_at(arg0 int32, arg1 uintptr, arg2 uint32, arg3 uintptr) int32

func (self *Descriptor) CreateDirectoryAt(path string) witTypes.Result[witTypes.Unit, ErrorCode] {
	pinner := &runtime.Pinner{}
	defer pinner.Unpin()

	returnArea := uintptr(witRuntime.Allocate(pinner, (5 * 4), 4))
	utf8 := unsafe.Pointer(unsafe.StringData(path))
	pinner.Pin(utf8)

	witAsync.SubtaskWait(uint32(wasm_import_method_descriptor_create_directory_at((self).Handle(), uintptr(utf8), uint32(len(path)), returnArea)))
	var result witTypes.Result[witTypes.Unit, ErrorCode]
	switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 0))) {
	case 0:

		result = witTypes.Ok[witTypes.Unit, ErrorCode](witTypes.Unit{})
	case 1:
		var variant ErrorCode
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 4))) {
		case 0:

			variant = MakeErrorCodeAccess()

		case 1:

			variant = MakeErrorCodeAlready()

		case 2:

			variant = MakeErrorCodeBadDescriptor()

		case 3:

			variant = MakeErrorCodeBusy()

		case 4:

			variant = MakeErrorCodeDeadlock()

		case 5:

			variant = MakeErrorCodeQuota()

		case 6:

			variant = MakeErrorCodeExist()

		case 7:

			variant = MakeErrorCodeFileTooLarge()

		case 8:

			variant = MakeErrorCodeIllegalByteSequence()

		case 9:

			variant = MakeErrorCodeInProgress()

		case 10:

			variant = MakeErrorCodeInterrupted()

		case 11:

			variant = MakeErrorCodeInvalid()

		case 12:

			variant = MakeErrorCodeIo()

		case 13:

			variant = MakeErrorCodeIsDirectory()

		case 14:

			variant = MakeErrorCodeLoop()

		case 15:

			variant = MakeErrorCodeTooManyLinks()

		case 16:

			variant = MakeErrorCodeMessageSize()

		case 17:

			variant = MakeErrorCodeNameTooLong()

		case 18:

			variant = MakeErrorCodeNoDevice()

		case 19:

			variant = MakeErrorCodeNoEntry()

		case 20:

			variant = MakeErrorCodeNoLock()

		case 21:

			variant = MakeErrorCodeInsufficientMemory()

		case 22:

			variant = MakeErrorCodeInsufficientSpace()

		case 23:

			variant = MakeErrorCodeNotDirectory()

		case 24:

			variant = MakeErrorCodeNotEmpty()

		case 25:

			variant = MakeErrorCodeNotRecoverable()

		case 26:

			variant = MakeErrorCodeUnsupported()

		case 27:

			variant = MakeErrorCodeNoTty()

		case 28:

			variant = MakeErrorCodeNoSuchDevice()

		case 29:

			variant = MakeErrorCodeOverflow()

		case 30:

			variant = MakeErrorCodeNotPermitted()

		case 31:

			variant = MakeErrorCodePipe()

		case 32:

			variant = MakeErrorCodeReadOnly()

		case 33:

			variant = MakeErrorCodeInvalidSeek()

		case 34:

			variant = MakeErrorCodeTextFileBusy()

		case 35:

			variant = MakeErrorCodeCrossDevice()

		case 36:
			var option witTypes.Option[string]
			switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (2 * 4)))) {
			case 0:

				option = witTypes.None[string]()
			case 1:
				value := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (3 * 4)))))), *(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (4 * 4))))

				option = witTypes.Some[string](value)
			default:
				panic("unreachable")
			}

			variant = MakeErrorCodeOther(option)

		default:
			panic("unreachable")
		}

		result = witTypes.Err[witTypes.Unit, ErrorCode](variant)
	default:
		panic("unreachable")
	}

	return result

}

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][method]descriptor.stat
func wasm_import_method_descriptor_stat(arg0 int32, arg1 uintptr) int32

func (self *Descriptor) Stat() witTypes.Result[DescriptorStat, ErrorCode] {
	pinner := &runtime.Pinner{}
	defer pinner.Unpin()

	returnArea := uintptr(witRuntime.Allocate(pinner, (96 + 4*4), 8))

	witAsync.SubtaskWait(uint32(wasm_import_method_descriptor_stat((self).Handle(), returnArea)))
	var result witTypes.Result[DescriptorStat, ErrorCode]
	switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 0))) {
	case 0:
		var variant DescriptorType
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 8))) {
		case 0:

			variant = MakeDescriptorTypeBlockDevice()

		case 1:

			variant = MakeDescriptorTypeCharacterDevice()

		case 2:

			variant = MakeDescriptorTypeDirectory()

		case 3:

			variant = MakeDescriptorTypeFifo()

		case 4:

			variant = MakeDescriptorTypeSymbolicLink()

		case 5:

			variant = MakeDescriptorTypeRegularFile()

		case 6:

			variant = MakeDescriptorTypeSocket()

		case 7:
			var option witTypes.Option[string]
			switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (8 + 1*4)))) {
			case 0:

				option = witTypes.None[string]()
			case 1:
				value := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (8 + 2*4)))))), *(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (8 + 3*4))))

				option = witTypes.Some[string](value)
			default:
				panic("unreachable")
			}

			variant = MakeDescriptorTypeOther(option)

		default:
			panic("unreachable")
		}
		var option0 witTypes.Option[wasi_clocks_system_clock.Instant]
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (24 + 4*4)))) {
		case 0:

			option0 = witTypes.None[wasi_clocks_system_clock.Instant]()
		case 1:

			option0 = witTypes.Some[wasi_clocks_system_clock.Instant](wasi_clocks_system_clock.Instant{*(*int64)(unsafe.Add(unsafe.Pointer(returnArea), (32 + 4*4))), uint32(*(*int32)(unsafe.Add(unsafe.Pointer(returnArea), (40 + 4*4))))})
		default:
			panic("unreachable")
		}
		var option1 witTypes.Option[wasi_clocks_system_clock.Instant]
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (48 + 4*4)))) {
		case 0:

			option1 = witTypes.None[wasi_clocks_system_clock.Instant]()
		case 1:

			option1 = witTypes.Some[wasi_clocks_system_clock.Instant](wasi_clocks_system_clock.Instant{*(*int64)(unsafe.Add(unsafe.Pointer(returnArea), (56 + 4*4))), uint32(*(*int32)(unsafe.Add(unsafe.Pointer(returnArea), (64 + 4*4))))})
		default:
			panic("unreachable")
		}
		var option2 witTypes.Option[wasi_clocks_system_clock.Instant]
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (72 + 4*4)))) {
		case 0:

			option2 = witTypes.None[wasi_clocks_system_clock.Instant]()
		case 1:

			option2 = witTypes.Some[wasi_clocks_system_clock.Instant](wasi_clocks_system_clock.Instant{*(*int64)(unsafe.Add(unsafe.Pointer(returnArea), (80 + 4*4))), uint32(*(*int32)(unsafe.Add(unsafe.Pointer(returnArea), (88 + 4*4))))})
		default:
			panic("unreachable")
		}

		result = witTypes.Ok[DescriptorStat, ErrorCode](DescriptorStat{variant, uint64(*(*int64)(unsafe.Add(unsafe.Pointer(returnArea), (8 + 4*4)))), uint64(*(*int64)(unsafe.Add(unsafe.Pointer(returnArea), (16 + 4*4)))), option0, option1, option2})
	case 1:
		var variant5 ErrorCode
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 8))) {
		case 0:

			variant5 = MakeErrorCodeAccess()

		case 1:

			variant5 = MakeErrorCodeAlready()

		case 2:

			variant5 = MakeErrorCodeBadDescriptor()

		case 3:

			variant5 = MakeErrorCodeBusy()

		case 4:

			variant5 = MakeErrorCodeDeadlock()

		case 5:

			variant5 = MakeErrorCodeQuota()

		case 6:

			variant5 = MakeErrorCodeExist()

		case 7:

			variant5 = MakeErrorCodeFileTooLarge()

		case 8:

			variant5 = MakeErrorCodeIllegalByteSequence()

		case 9:

			variant5 = MakeErrorCodeInProgress()

		case 10:

			variant5 = MakeErrorCodeInterrupted()

		case 11:

			variant5 = MakeErrorCodeInvalid()

		case 12:

			variant5 = MakeErrorCodeIo()

		case 13:

			variant5 = MakeErrorCodeIsDirectory()

		case 14:

			variant5 = MakeErrorCodeLoop()

		case 15:

			variant5 = MakeErrorCodeTooManyLinks()

		case 16:

			variant5 = MakeErrorCodeMessageSize()

		case 17:

			variant5 = MakeErrorCodeNameTooLong()

		case 18:

			variant5 = MakeErrorCodeNoDevice()

		case 19:

			variant5 = MakeErrorCodeNoEntry()

		case 20:

			variant5 = MakeErrorCodeNoLock()

		case 21:

			variant5 = MakeErrorCodeInsufficientMemory()

		case 22:

			variant5 = MakeErrorCodeInsufficientSpace()

		case 23:

			variant5 = MakeErrorCodeNotDirectory()

		case 24:

			variant5 = MakeErrorCodeNotEmpty()

		case 25:

			variant5 = MakeErrorCodeNotRecoverable()

		case 26:

			variant5 = MakeErrorCodeUnsupported()

		case 27:

			variant5 = MakeErrorCodeNoTty()

		case 28:

			variant5 = MakeErrorCodeNoSuchDevice()

		case 29:

			variant5 = MakeErrorCodeOverflow()

		case 30:

			variant5 = MakeErrorCodeNotPermitted()

		case 31:

			variant5 = MakeErrorCodePipe()

		case 32:

			variant5 = MakeErrorCodeReadOnly()

		case 33:

			variant5 = MakeErrorCodeInvalidSeek()

		case 34:

			variant5 = MakeErrorCodeTextFileBusy()

		case 35:

			variant5 = MakeErrorCodeCrossDevice()

		case 36:
			var option4 witTypes.Option[string]
			switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (8 + 1*4)))) {
			case 0:

				option4 = witTypes.None[string]()
			case 1:
				value3 := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (8 + 2*4)))))), *(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (8 + 3*4))))

				option4 = witTypes.Some[string](value3)
			default:
				panic("unreachable")
			}

			variant5 = MakeErrorCodeOther(option4)

		default:
			panic("unreachable")
		}

		result = witTypes.Err[DescriptorStat, ErrorCode](variant5)
	default:
		panic("unreachable")
	}

	return result

}

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][method]descriptor.stat-at
func wasm_import_method_descriptor_stat_at(arg0 int32, arg1 int32, arg2 uintptr, arg3 uint32, arg4 uintptr) int32

func (self *Descriptor) StatAt(pathFlags PathFlags, path string) witTypes.Result[DescriptorStat, ErrorCode] {
	pinner := &runtime.Pinner{}
	defer pinner.Unpin()

	returnArea := uintptr(witRuntime.Allocate(pinner, (96 + 4*4), 8))
	utf8 := unsafe.Pointer(unsafe.StringData(path))
	pinner.Pin(utf8)

	witAsync.SubtaskWait(uint32(wasm_import_method_descriptor_stat_at((self).Handle(), int32(pathFlags), uintptr(utf8), uint32(len(path)), returnArea)))
	var result witTypes.Result[DescriptorStat, ErrorCode]
	switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 0))) {
	case 0:
		var variant DescriptorType
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 8))) {
		case 0:

			variant = MakeDescriptorTypeBlockDevice()

		case 1:

			variant = MakeDescriptorTypeCharacterDevice()

		case 2:

			variant = MakeDescriptorTypeDirectory()

		case 3:

			variant = MakeDescriptorTypeFifo()

		case 4:

			variant = MakeDescriptorTypeSymbolicLink()

		case 5:

			variant = MakeDescriptorTypeRegularFile()

		case 6:

			variant = MakeDescriptorTypeSocket()

		case 7:
			var option witTypes.Option[string]
			switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (8 + 1*4)))) {
			case 0:

				option = witTypes.None[string]()
			case 1:
				value := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (8 + 2*4)))))), *(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (8 + 3*4))))

				option = witTypes.Some[string](value)
			default:
				panic("unreachable")
			}

			variant = MakeDescriptorTypeOther(option)

		default:
			panic("unreachable")
		}
		var option0 witTypes.Option[wasi_clocks_system_clock.Instant]
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (24 + 4*4)))) {
		case 0:

			option0 = witTypes.None[wasi_clocks_system_clock.Instant]()
		case 1:

			option0 = witTypes.Some[wasi_clocks_system_clock.Instant](wasi_clocks_system_clock.Instant{*(*int64)(unsafe.Add(unsafe.Pointer(returnArea), (32 + 4*4))), uint32(*(*int32)(unsafe.Add(unsafe.Pointer(returnArea), (40 + 4*4))))})
		default:
			panic("unreachable")
		}
		var option1 witTypes.Option[wasi_clocks_system_clock.Instant]
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (48 + 4*4)))) {
		case 0:

			option1 = witTypes.None[wasi_clocks_system_clock.Instant]()
		case 1:

			option1 = witTypes.Some[wasi_clocks_system_clock.Instant](wasi_clocks_system_clock.Instant{*(*int64)(unsafe.Add(unsafe.Pointer(returnArea), (56 + 4*4))), uint32(*(*int32)(unsafe.Add(unsafe.Pointer(returnArea), (64 + 4*4))))})
		default:
			panic("unreachable")
		}
		var option2 witTypes.Option[wasi_clocks_system_clock.Instant]
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (72 + 4*4)))) {
		case 0:

			option2 = witTypes.None[wasi_clocks_system_clock.Instant]()
		case 1:

			option2 = witTypes.Some[wasi_clocks_system_clock.Instant](wasi_clocks_system_clock.Instant{*(*int64)(unsafe.Add(unsafe.Pointer(returnArea), (80 + 4*4))), uint32(*(*int32)(unsafe.Add(unsafe.Pointer(returnArea), (88 + 4*4))))})
		default:
			panic("unreachable")
		}

		result = witTypes.Ok[DescriptorStat, ErrorCode](DescriptorStat{variant, uint64(*(*int64)(unsafe.Add(unsafe.Pointer(returnArea), (8 + 4*4)))), uint64(*(*int64)(unsafe.Add(unsafe.Pointer(returnArea), (16 + 4*4)))), option0, option1, option2})
	case 1:
		var variant5 ErrorCode
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 8))) {
		case 0:

			variant5 = MakeErrorCodeAccess()

		case 1:

			variant5 = MakeErrorCodeAlready()

		case 2:

			variant5 = MakeErrorCodeBadDescriptor()

		case 3:

			variant5 = MakeErrorCodeBusy()

		case 4:

			variant5 = MakeErrorCodeDeadlock()

		case 5:

			variant5 = MakeErrorCodeQuota()

		case 6:

			variant5 = MakeErrorCodeExist()

		case 7:

			variant5 = MakeErrorCodeFileTooLarge()

		case 8:

			variant5 = MakeErrorCodeIllegalByteSequence()

		case 9:

			variant5 = MakeErrorCodeInProgress()

		case 10:

			variant5 = MakeErrorCodeInterrupted()

		case 11:

			variant5 = MakeErrorCodeInvalid()

		case 12:

			variant5 = MakeErrorCodeIo()

		case 13:

			variant5 = MakeErrorCodeIsDirectory()

		case 14:

			variant5 = MakeErrorCodeLoop()

		case 15:

			variant5 = MakeErrorCodeTooManyLinks()

		case 16:

			variant5 = MakeErrorCodeMessageSize()

		case 17:

			variant5 = MakeErrorCodeNameTooLong()

		case 18:

			variant5 = MakeErrorCodeNoDevice()

		case 19:

			variant5 = MakeErrorCodeNoEntry()

		case 20:

			variant5 = MakeErrorCodeNoLock()

		case 21:

			variant5 = MakeErrorCodeInsufficientMemory()

		case 22:

			variant5 = MakeErrorCodeInsufficientSpace()

		case 23:

			variant5 = MakeErrorCodeNotDirectory()

		case 24:

			variant5 = MakeErrorCodeNotEmpty()

		case 25:

			variant5 = MakeErrorCodeNotRecoverable()

		case 26:

			variant5 = MakeErrorCodeUnsupported()

		case 27:

			variant5 = MakeErrorCodeNoTty()

		case 28:

			variant5 = MakeErrorCodeNoSuchDevice()

		case 29:

			variant5 = MakeErrorCodeOverflow()

		case 30:

			variant5 = MakeErrorCodeNotPermitted()

		case 31:

			variant5 = MakeErrorCodePipe()

		case 32:

			variant5 = MakeErrorCodeReadOnly()

		case 33:

			variant5 = MakeErrorCodeInvalidSeek()

		case 34:

			variant5 = MakeErrorCodeTextFileBusy()

		case 35:

			variant5 = MakeErrorCodeCrossDevice()

		case 36:
			var option4 witTypes.Option[string]
			switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (8 + 1*4)))) {
			case 0:

				option4 = witTypes.None[string]()
			case 1:
				value3 := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (8 + 2*4)))))), *(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (8 + 3*4))))

				option4 = witTypes.Some[string](value3)
			default:
				panic("unreachable")
			}

			variant5 = MakeErrorCodeOther(option4)

		default:
			panic("unreachable")
		}

		result = witTypes.Err[DescriptorStat, ErrorCode](variant5)
	default:
		panic("unreachable")
	}

	return result

}

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][method]descriptor.set-times-at
func wasm_import_method_descriptor_set_times_at(arg0 uintptr, arg1 uintptr) int32

func (self *Descriptor) SetTimesAt(pathFlags PathFlags, path string, dataAccessTimestamp NewTimestamp, dataModificationTimestamp NewTimestamp) witTypes.Result[witTypes.Unit, ErrorCode] {
	pinner := &runtime.Pinner{}
	defer pinner.Unpin()

	returnArea := uintptr(witRuntime.Allocate(pinner, (5 * 4), 4))
	params := witRuntime.Allocate(pinner, (56 + 2*4), 8)
	*(*int32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 0)), 0)) = (self).Handle()
	*(*int8)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 4)), 0)) = int8(int32(pathFlags))
	utf8 := unsafe.Pointer(unsafe.StringData(path))
	pinner.Pin(utf8)
	*(*uint32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 8)), 4)) = uint32(uint32(len(path)))
	*(*uint32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 8)), 0)) = uint32(uintptr(uintptr(utf8)))

	switch dataAccessTimestamp.Tag() {
	case NewTimestampNoChange:

		*(*int8)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), (8+2*4))), 0)) = int8(int32(0))

	case NewTimestampNow:

		*(*int8)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), (8+2*4))), 0)) = int8(int32(1))

	case NewTimestampTimestamp:
		payload := dataAccessTimestamp.Timestamp()
		*(*int8)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), (8+2*4))), 0)) = int8(int32(2))
		*(*int64)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), (8+2*4))), 8)) = (payload).Seconds
		*(*int32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), (8+2*4))), 16)) = int32((payload).Nanoseconds)

	default:
		panic("unreachable")
	}

	switch dataModificationTimestamp.Tag() {
	case NewTimestampNoChange:

		*(*int8)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), (32+2*4))), 0)) = int8(int32(0))

	case NewTimestampNow:

		*(*int8)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), (32+2*4))), 0)) = int8(int32(1))

	case NewTimestampTimestamp:
		payload := dataModificationTimestamp.Timestamp()
		*(*int8)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), (32+2*4))), 0)) = int8(int32(2))
		*(*int64)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), (32+2*4))), 8)) = (payload).Seconds
		*(*int32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), (32+2*4))), 16)) = int32((payload).Nanoseconds)

	default:
		panic("unreachable")
	}

	witAsync.SubtaskWait(uint32(wasm_import_method_descriptor_set_times_at(uintptr(params), returnArea)))
	var result witTypes.Result[witTypes.Unit, ErrorCode]
	switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 0))) {
	case 0:

		result = witTypes.Ok[witTypes.Unit, ErrorCode](witTypes.Unit{})
	case 1:
		var variant ErrorCode
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 4))) {
		case 0:

			variant = MakeErrorCodeAccess()

		case 1:

			variant = MakeErrorCodeAlready()

		case 2:

			variant = MakeErrorCodeBadDescriptor()

		case 3:

			variant = MakeErrorCodeBusy()

		case 4:

			variant = MakeErrorCodeDeadlock()

		case 5:

			variant = MakeErrorCodeQuota()

		case 6:

			variant = MakeErrorCodeExist()

		case 7:

			variant = MakeErrorCodeFileTooLarge()

		case 8:

			variant = MakeErrorCodeIllegalByteSequence()

		case 9:

			variant = MakeErrorCodeInProgress()

		case 10:

			variant = MakeErrorCodeInterrupted()

		case 11:

			variant = MakeErrorCodeInvalid()

		case 12:

			variant = MakeErrorCodeIo()

		case 13:

			variant = MakeErrorCodeIsDirectory()

		case 14:

			variant = MakeErrorCodeLoop()

		case 15:

			variant = MakeErrorCodeTooManyLinks()

		case 16:

			variant = MakeErrorCodeMessageSize()

		case 17:

			variant = MakeErrorCodeNameTooLong()

		case 18:

			variant = MakeErrorCodeNoDevice()

		case 19:

			variant = MakeErrorCodeNoEntry()

		case 20:

			variant = MakeErrorCodeNoLock()

		case 21:

			variant = MakeErrorCodeInsufficientMemory()

		case 22:

			variant = MakeErrorCodeInsufficientSpace()

		case 23:

			variant = MakeErrorCodeNotDirectory()

		case 24:

			variant = MakeErrorCodeNotEmpty()

		case 25:

			variant = MakeErrorCodeNotRecoverable()

		case 26:

			variant = MakeErrorCodeUnsupported()

		case 27:

			variant = MakeErrorCodeNoTty()

		case 28:

			variant = MakeErrorCodeNoSuchDevice()

		case 29:

			variant = MakeErrorCodeOverflow()

		case 30:

			variant = MakeErrorCodeNotPermitted()

		case 31:

			variant = MakeErrorCodePipe()

		case 32:

			variant = MakeErrorCodeReadOnly()

		case 33:

			variant = MakeErrorCodeInvalidSeek()

		case 34:

			variant = MakeErrorCodeTextFileBusy()

		case 35:

			variant = MakeErrorCodeCrossDevice()

		case 36:
			var option witTypes.Option[string]
			switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (2 * 4)))) {
			case 0:

				option = witTypes.None[string]()
			case 1:
				value := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (3 * 4)))))), *(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (4 * 4))))

				option = witTypes.Some[string](value)
			default:
				panic("unreachable")
			}

			variant = MakeErrorCodeOther(option)

		default:
			panic("unreachable")
		}

		result = witTypes.Err[witTypes.Unit, ErrorCode](variant)
	default:
		panic("unreachable")
	}

	return result

}

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][method]descriptor.link-at
func wasm_import_method_descriptor_link_at(arg0 uintptr, arg1 uintptr) int32

func (self *Descriptor) LinkAt(oldPathFlags PathFlags, oldPath string, newDescriptor *Descriptor, newPath string) witTypes.Result[witTypes.Unit, ErrorCode] {
	pinner := &runtime.Pinner{}
	defer pinner.Unpin()

	returnArea := uintptr(witRuntime.Allocate(pinner, (5 * 4), 4))
	params := witRuntime.Allocate(pinner, (8 + 5*4), 4)
	*(*int32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 0)), 0)) = (self).Handle()
	*(*int8)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 4)), 0)) = int8(int32(oldPathFlags))
	utf8 := unsafe.Pointer(unsafe.StringData(oldPath))
	pinner.Pin(utf8)
	*(*uint32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 8)), 4)) = uint32(uint32(len(oldPath)))
	*(*uint32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 8)), 0)) = uint32(uintptr(uintptr(utf8)))
	*(*int32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), (8+2*4))), 0)) = (newDescriptor).Handle()
	utf80 := unsafe.Pointer(unsafe.StringData(newPath))
	pinner.Pin(utf80)
	*(*uint32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), (8+3*4))), 4)) = uint32(uint32(len(newPath)))
	*(*uint32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), (8+3*4))), 0)) = uint32(uintptr(uintptr(utf80)))

	witAsync.SubtaskWait(uint32(wasm_import_method_descriptor_link_at(uintptr(params), returnArea)))
	var result witTypes.Result[witTypes.Unit, ErrorCode]
	switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 0))) {
	case 0:

		result = witTypes.Ok[witTypes.Unit, ErrorCode](witTypes.Unit{})
	case 1:
		var variant ErrorCode
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 4))) {
		case 0:

			variant = MakeErrorCodeAccess()

		case 1:

			variant = MakeErrorCodeAlready()

		case 2:

			variant = MakeErrorCodeBadDescriptor()

		case 3:

			variant = MakeErrorCodeBusy()

		case 4:

			variant = MakeErrorCodeDeadlock()

		case 5:

			variant = MakeErrorCodeQuota()

		case 6:

			variant = MakeErrorCodeExist()

		case 7:

			variant = MakeErrorCodeFileTooLarge()

		case 8:

			variant = MakeErrorCodeIllegalByteSequence()

		case 9:

			variant = MakeErrorCodeInProgress()

		case 10:

			variant = MakeErrorCodeInterrupted()

		case 11:

			variant = MakeErrorCodeInvalid()

		case 12:

			variant = MakeErrorCodeIo()

		case 13:

			variant = MakeErrorCodeIsDirectory()

		case 14:

			variant = MakeErrorCodeLoop()

		case 15:

			variant = MakeErrorCodeTooManyLinks()

		case 16:

			variant = MakeErrorCodeMessageSize()

		case 17:

			variant = MakeErrorCodeNameTooLong()

		case 18:

			variant = MakeErrorCodeNoDevice()

		case 19:

			variant = MakeErrorCodeNoEntry()

		case 20:

			variant = MakeErrorCodeNoLock()

		case 21:

			variant = MakeErrorCodeInsufficientMemory()

		case 22:

			variant = MakeErrorCodeInsufficientSpace()

		case 23:

			variant = MakeErrorCodeNotDirectory()

		case 24:

			variant = MakeErrorCodeNotEmpty()

		case 25:

			variant = MakeErrorCodeNotRecoverable()

		case 26:

			variant = MakeErrorCodeUnsupported()

		case 27:

			variant = MakeErrorCodeNoTty()

		case 28:

			variant = MakeErrorCodeNoSuchDevice()

		case 29:

			variant = MakeErrorCodeOverflow()

		case 30:

			variant = MakeErrorCodeNotPermitted()

		case 31:

			variant = MakeErrorCodePipe()

		case 32:

			variant = MakeErrorCodeReadOnly()

		case 33:

			variant = MakeErrorCodeInvalidSeek()

		case 34:

			variant = MakeErrorCodeTextFileBusy()

		case 35:

			variant = MakeErrorCodeCrossDevice()

		case 36:
			var option witTypes.Option[string]
			switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (2 * 4)))) {
			case 0:

				option = witTypes.None[string]()
			case 1:
				value := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (3 * 4)))))), *(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (4 * 4))))

				option = witTypes.Some[string](value)
			default:
				panic("unreachable")
			}

			variant = MakeErrorCodeOther(option)

		default:
			panic("unreachable")
		}

		result = witTypes.Err[witTypes.Unit, ErrorCode](variant)
	default:
		panic("unreachable")
	}

	return result

}

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][method]descriptor.open-at
func wasm_import_method_descriptor_open_at(arg0 uintptr, arg1 uintptr) int32

func (self *Descriptor) OpenAt(pathFlags PathFlags, path string, openFlags OpenFlags, flags DescriptorFlags) witTypes.Result[*Descriptor, ErrorCode] {
	pinner := &runtime.Pinner{}
	defer pinner.Unpin()

	returnArea := uintptr(witRuntime.Allocate(pinner, (5 * 4), 4))
	params := witRuntime.Allocate(pinner, (8 + 3*4), 4)
	*(*int32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 0)), 0)) = (self).Handle()
	*(*int8)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 4)), 0)) = int8(int32(pathFlags))
	utf8 := unsafe.Pointer(unsafe.StringData(path))
	pinner.Pin(utf8)
	*(*uint32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 8)), 4)) = uint32(uint32(len(path)))
	*(*uint32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 8)), 0)) = uint32(uintptr(uintptr(utf8)))
	*(*int8)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), (8+2*4))), 0)) = int8(int32(openFlags))
	*(*int8)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), (9+2*4))), 0)) = int8(int32(flags))

	witAsync.SubtaskWait(uint32(wasm_import_method_descriptor_open_at(uintptr(params), returnArea)))
	var result witTypes.Result[*Descriptor, ErrorCode]
	switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 0))) {
	case 0:

		result = witTypes.Ok[*Descriptor, ErrorCode](DescriptorFromOwnHandle(int32(uintptr(*(*int32)(unsafe.Add(unsafe.Pointer(returnArea), 4))))))
	case 1:
		var variant ErrorCode
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 4))) {
		case 0:

			variant = MakeErrorCodeAccess()

		case 1:

			variant = MakeErrorCodeAlready()

		case 2:

			variant = MakeErrorCodeBadDescriptor()

		case 3:

			variant = MakeErrorCodeBusy()

		case 4:

			variant = MakeErrorCodeDeadlock()

		case 5:

			variant = MakeErrorCodeQuota()

		case 6:

			variant = MakeErrorCodeExist()

		case 7:

			variant = MakeErrorCodeFileTooLarge()

		case 8:

			variant = MakeErrorCodeIllegalByteSequence()

		case 9:

			variant = MakeErrorCodeInProgress()

		case 10:

			variant = MakeErrorCodeInterrupted()

		case 11:

			variant = MakeErrorCodeInvalid()

		case 12:

			variant = MakeErrorCodeIo()

		case 13:

			variant = MakeErrorCodeIsDirectory()

		case 14:

			variant = MakeErrorCodeLoop()

		case 15:

			variant = MakeErrorCodeTooManyLinks()

		case 16:

			variant = MakeErrorCodeMessageSize()

		case 17:

			variant = MakeErrorCodeNameTooLong()

		case 18:

			variant = MakeErrorCodeNoDevice()

		case 19:

			variant = MakeErrorCodeNoEntry()

		case 20:

			variant = MakeErrorCodeNoLock()

		case 21:

			variant = MakeErrorCodeInsufficientMemory()

		case 22:

			variant = MakeErrorCodeInsufficientSpace()

		case 23:

			variant = MakeErrorCodeNotDirectory()

		case 24:

			variant = MakeErrorCodeNotEmpty()

		case 25:

			variant = MakeErrorCodeNotRecoverable()

		case 26:

			variant = MakeErrorCodeUnsupported()

		case 27:

			variant = MakeErrorCodeNoTty()

		case 28:

			variant = MakeErrorCodeNoSuchDevice()

		case 29:

			variant = MakeErrorCodeOverflow()

		case 30:

			variant = MakeErrorCodeNotPermitted()

		case 31:

			variant = MakeErrorCodePipe()

		case 32:

			variant = MakeErrorCodeReadOnly()

		case 33:

			variant = MakeErrorCodeInvalidSeek()

		case 34:

			variant = MakeErrorCodeTextFileBusy()

		case 35:

			variant = MakeErrorCodeCrossDevice()

		case 36:
			var option witTypes.Option[string]
			switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (2 * 4)))) {
			case 0:

				option = witTypes.None[string]()
			case 1:
				value := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (3 * 4)))))), *(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (4 * 4))))

				option = witTypes.Some[string](value)
			default:
				panic("unreachable")
			}

			variant = MakeErrorCodeOther(option)

		default:
			panic("unreachable")
		}

		result = witTypes.Err[*Descriptor, ErrorCode](variant)
	default:
		panic("unreachable")
	}

	return result

}

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][method]descriptor.readlink-at
func wasm_import_method_descriptor_readlink_at(arg0 int32, arg1 uintptr, arg2 uint32, arg3 uintptr) int32

func (self *Descriptor) ReadlinkAt(path string) witTypes.Result[string, ErrorCode] {
	pinner := &runtime.Pinner{}
	defer pinner.Unpin()

	returnArea := uintptr(witRuntime.Allocate(pinner, (5 * 4), 4))
	utf8 := unsafe.Pointer(unsafe.StringData(path))
	pinner.Pin(utf8)

	witAsync.SubtaskWait(uint32(wasm_import_method_descriptor_readlink_at((self).Handle(), uintptr(utf8), uint32(len(path)), returnArea)))
	var result witTypes.Result[string, ErrorCode]
	switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 0))) {
	case 0:
		value := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 4))))), *(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (2 * 4))))

		result = witTypes.Ok[string, ErrorCode](value)
	case 1:
		var variant ErrorCode
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 4))) {
		case 0:

			variant = MakeErrorCodeAccess()

		case 1:

			variant = MakeErrorCodeAlready()

		case 2:

			variant = MakeErrorCodeBadDescriptor()

		case 3:

			variant = MakeErrorCodeBusy()

		case 4:

			variant = MakeErrorCodeDeadlock()

		case 5:

			variant = MakeErrorCodeQuota()

		case 6:

			variant = MakeErrorCodeExist()

		case 7:

			variant = MakeErrorCodeFileTooLarge()

		case 8:

			variant = MakeErrorCodeIllegalByteSequence()

		case 9:

			variant = MakeErrorCodeInProgress()

		case 10:

			variant = MakeErrorCodeInterrupted()

		case 11:

			variant = MakeErrorCodeInvalid()

		case 12:

			variant = MakeErrorCodeIo()

		case 13:

			variant = MakeErrorCodeIsDirectory()

		case 14:

			variant = MakeErrorCodeLoop()

		case 15:

			variant = MakeErrorCodeTooManyLinks()

		case 16:

			variant = MakeErrorCodeMessageSize()

		case 17:

			variant = MakeErrorCodeNameTooLong()

		case 18:

			variant = MakeErrorCodeNoDevice()

		case 19:

			variant = MakeErrorCodeNoEntry()

		case 20:

			variant = MakeErrorCodeNoLock()

		case 21:

			variant = MakeErrorCodeInsufficientMemory()

		case 22:

			variant = MakeErrorCodeInsufficientSpace()

		case 23:

			variant = MakeErrorCodeNotDirectory()

		case 24:

			variant = MakeErrorCodeNotEmpty()

		case 25:

			variant = MakeErrorCodeNotRecoverable()

		case 26:

			variant = MakeErrorCodeUnsupported()

		case 27:

			variant = MakeErrorCodeNoTty()

		case 28:

			variant = MakeErrorCodeNoSuchDevice()

		case 29:

			variant = MakeErrorCodeOverflow()

		case 30:

			variant = MakeErrorCodeNotPermitted()

		case 31:

			variant = MakeErrorCodePipe()

		case 32:

			variant = MakeErrorCodeReadOnly()

		case 33:

			variant = MakeErrorCodeInvalidSeek()

		case 34:

			variant = MakeErrorCodeTextFileBusy()

		case 35:

			variant = MakeErrorCodeCrossDevice()

		case 36:
			var option witTypes.Option[string]
			switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (2 * 4)))) {
			case 0:

				option = witTypes.None[string]()
			case 1:
				value0 := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (3 * 4)))))), *(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (4 * 4))))

				option = witTypes.Some[string](value0)
			default:
				panic("unreachable")
			}

			variant = MakeErrorCodeOther(option)

		default:
			panic("unreachable")
		}

		result = witTypes.Err[string, ErrorCode](variant)
	default:
		panic("unreachable")
	}

	return result

}

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][method]descriptor.remove-directory-at
func wasm_import_method_descriptor_remove_directory_at(arg0 int32, arg1 uintptr, arg2 uint32, arg3 uintptr) int32

func (self *Descriptor) RemoveDirectoryAt(path string) witTypes.Result[witTypes.Unit, ErrorCode] {
	pinner := &runtime.Pinner{}
	defer pinner.Unpin()

	returnArea := uintptr(witRuntime.Allocate(pinner, (5 * 4), 4))
	utf8 := unsafe.Pointer(unsafe.StringData(path))
	pinner.Pin(utf8)

	witAsync.SubtaskWait(uint32(wasm_import_method_descriptor_remove_directory_at((self).Handle(), uintptr(utf8), uint32(len(path)), returnArea)))
	var result witTypes.Result[witTypes.Unit, ErrorCode]
	switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 0))) {
	case 0:

		result = witTypes.Ok[witTypes.Unit, ErrorCode](witTypes.Unit{})
	case 1:
		var variant ErrorCode
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 4))) {
		case 0:

			variant = MakeErrorCodeAccess()

		case 1:

			variant = MakeErrorCodeAlready()

		case 2:

			variant = MakeErrorCodeBadDescriptor()

		case 3:

			variant = MakeErrorCodeBusy()

		case 4:

			variant = MakeErrorCodeDeadlock()

		case 5:

			variant = MakeErrorCodeQuota()

		case 6:

			variant = MakeErrorCodeExist()

		case 7:

			variant = MakeErrorCodeFileTooLarge()

		case 8:

			variant = MakeErrorCodeIllegalByteSequence()

		case 9:

			variant = MakeErrorCodeInProgress()

		case 10:

			variant = MakeErrorCodeInterrupted()

		case 11:

			variant = MakeErrorCodeInvalid()

		case 12:

			variant = MakeErrorCodeIo()

		case 13:

			variant = MakeErrorCodeIsDirectory()

		case 14:

			variant = MakeErrorCodeLoop()

		case 15:

			variant = MakeErrorCodeTooManyLinks()

		case 16:

			variant = MakeErrorCodeMessageSize()

		case 17:

			variant = MakeErrorCodeNameTooLong()

		case 18:

			variant = MakeErrorCodeNoDevice()

		case 19:

			variant = MakeErrorCodeNoEntry()

		case 20:

			variant = MakeErrorCodeNoLock()

		case 21:

			variant = MakeErrorCodeInsufficientMemory()

		case 22:

			variant = MakeErrorCodeInsufficientSpace()

		case 23:

			variant = MakeErrorCodeNotDirectory()

		case 24:

			variant = MakeErrorCodeNotEmpty()

		case 25:

			variant = MakeErrorCodeNotRecoverable()

		case 26:

			variant = MakeErrorCodeUnsupported()

		case 27:

			variant = MakeErrorCodeNoTty()

		case 28:

			variant = MakeErrorCodeNoSuchDevice()

		case 29:

			variant = MakeErrorCodeOverflow()

		case 30:

			variant = MakeErrorCodeNotPermitted()

		case 31:

			variant = MakeErrorCodePipe()

		case 32:

			variant = MakeErrorCodeReadOnly()

		case 33:

			variant = MakeErrorCodeInvalidSeek()

		case 34:

			variant = MakeErrorCodeTextFileBusy()

		case 35:

			variant = MakeErrorCodeCrossDevice()

		case 36:
			var option witTypes.Option[string]
			switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (2 * 4)))) {
			case 0:

				option = witTypes.None[string]()
			case 1:
				value := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (3 * 4)))))), *(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (4 * 4))))

				option = witTypes.Some[string](value)
			default:
				panic("unreachable")
			}

			variant = MakeErrorCodeOther(option)

		default:
			panic("unreachable")
		}

		result = witTypes.Err[witTypes.Unit, ErrorCode](variant)
	default:
		panic("unreachable")
	}

	return result

}

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][method]descriptor.rename-at
func wasm_import_method_descriptor_rename_at(arg0 uintptr, arg1 uintptr) int32

func (self *Descriptor) RenameAt(oldPath string, newDescriptor *Descriptor, newPath string) witTypes.Result[witTypes.Unit, ErrorCode] {
	pinner := &runtime.Pinner{}
	defer pinner.Unpin()

	returnArea := uintptr(witRuntime.Allocate(pinner, (5 * 4), 4))
	params := witRuntime.Allocate(pinner, (6 * 4), 4)
	*(*int32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 0)), 0)) = (self).Handle()
	utf8 := unsafe.Pointer(unsafe.StringData(oldPath))
	pinner.Pin(utf8)
	*(*uint32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 4)), 4)) = uint32(uint32(len(oldPath)))
	*(*uint32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 4)), 0)) = uint32(uintptr(uintptr(utf8)))
	*(*int32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), (3*4))), 0)) = (newDescriptor).Handle()
	utf80 := unsafe.Pointer(unsafe.StringData(newPath))
	pinner.Pin(utf80)
	*(*uint32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), (4*4))), 4)) = uint32(uint32(len(newPath)))
	*(*uint32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), (4*4))), 0)) = uint32(uintptr(uintptr(utf80)))

	witAsync.SubtaskWait(uint32(wasm_import_method_descriptor_rename_at(uintptr(params), returnArea)))
	var result witTypes.Result[witTypes.Unit, ErrorCode]
	switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 0))) {
	case 0:

		result = witTypes.Ok[witTypes.Unit, ErrorCode](witTypes.Unit{})
	case 1:
		var variant ErrorCode
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 4))) {
		case 0:

			variant = MakeErrorCodeAccess()

		case 1:

			variant = MakeErrorCodeAlready()

		case 2:

			variant = MakeErrorCodeBadDescriptor()

		case 3:

			variant = MakeErrorCodeBusy()

		case 4:

			variant = MakeErrorCodeDeadlock()

		case 5:

			variant = MakeErrorCodeQuota()

		case 6:

			variant = MakeErrorCodeExist()

		case 7:

			variant = MakeErrorCodeFileTooLarge()

		case 8:

			variant = MakeErrorCodeIllegalByteSequence()

		case 9:

			variant = MakeErrorCodeInProgress()

		case 10:

			variant = MakeErrorCodeInterrupted()

		case 11:

			variant = MakeErrorCodeInvalid()

		case 12:

			variant = MakeErrorCodeIo()

		case 13:

			variant = MakeErrorCodeIsDirectory()

		case 14:

			variant = MakeErrorCodeLoop()

		case 15:

			variant = MakeErrorCodeTooManyLinks()

		case 16:

			variant = MakeErrorCodeMessageSize()

		case 17:

			variant = MakeErrorCodeNameTooLong()

		case 18:

			variant = MakeErrorCodeNoDevice()

		case 19:

			variant = MakeErrorCodeNoEntry()

		case 20:

			variant = MakeErrorCodeNoLock()

		case 21:

			variant = MakeErrorCodeInsufficientMemory()

		case 22:

			variant = MakeErrorCodeInsufficientSpace()

		case 23:

			variant = MakeErrorCodeNotDirectory()

		case 24:

			variant = MakeErrorCodeNotEmpty()

		case 25:

			variant = MakeErrorCodeNotRecoverable()

		case 26:

			variant = MakeErrorCodeUnsupported()

		case 27:

			variant = MakeErrorCodeNoTty()

		case 28:

			variant = MakeErrorCodeNoSuchDevice()

		case 29:

			variant = MakeErrorCodeOverflow()

		case 30:

			variant = MakeErrorCodeNotPermitted()

		case 31:

			variant = MakeErrorCodePipe()

		case 32:

			variant = MakeErrorCodeReadOnly()

		case 33:

			variant = MakeErrorCodeInvalidSeek()

		case 34:

			variant = MakeErrorCodeTextFileBusy()

		case 35:

			variant = MakeErrorCodeCrossDevice()

		case 36:
			var option witTypes.Option[string]
			switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (2 * 4)))) {
			case 0:

				option = witTypes.None[string]()
			case 1:
				value := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (3 * 4)))))), *(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (4 * 4))))

				option = witTypes.Some[string](value)
			default:
				panic("unreachable")
			}

			variant = MakeErrorCodeOther(option)

		default:
			panic("unreachable")
		}

		result = witTypes.Err[witTypes.Unit, ErrorCode](variant)
	default:
		panic("unreachable")
	}

	return result

}

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][method]descriptor.symlink-at
func wasm_import_method_descriptor_symlink_at(arg0 uintptr, arg1 uintptr) int32

func (self *Descriptor) SymlinkAt(oldPath string, newPath string) witTypes.Result[witTypes.Unit, ErrorCode] {
	pinner := &runtime.Pinner{}
	defer pinner.Unpin()

	returnArea := uintptr(witRuntime.Allocate(pinner, (5 * 4), 4))
	params := witRuntime.Allocate(pinner, (5 * 4), 4)
	*(*int32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 0)), 0)) = (self).Handle()
	utf8 := unsafe.Pointer(unsafe.StringData(oldPath))
	pinner.Pin(utf8)
	*(*uint32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 4)), 4)) = uint32(uint32(len(oldPath)))
	*(*uint32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), 4)), 0)) = uint32(uintptr(uintptr(utf8)))
	utf80 := unsafe.Pointer(unsafe.StringData(newPath))
	pinner.Pin(utf80)
	*(*uint32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), (3*4))), 4)) = uint32(uint32(len(newPath)))
	*(*uint32)(unsafe.Add(unsafe.Pointer(unsafe.Add(unsafe.Pointer(params), (3*4))), 0)) = uint32(uintptr(uintptr(utf80)))

	witAsync.SubtaskWait(uint32(wasm_import_method_descriptor_symlink_at(uintptr(params), returnArea)))
	var result witTypes.Result[witTypes.Unit, ErrorCode]
	switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 0))) {
	case 0:

		result = witTypes.Ok[witTypes.Unit, ErrorCode](witTypes.Unit{})
	case 1:
		var variant ErrorCode
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 4))) {
		case 0:

			variant = MakeErrorCodeAccess()

		case 1:

			variant = MakeErrorCodeAlready()

		case 2:

			variant = MakeErrorCodeBadDescriptor()

		case 3:

			variant = MakeErrorCodeBusy()

		case 4:

			variant = MakeErrorCodeDeadlock()

		case 5:

			variant = MakeErrorCodeQuota()

		case 6:

			variant = MakeErrorCodeExist()

		case 7:

			variant = MakeErrorCodeFileTooLarge()

		case 8:

			variant = MakeErrorCodeIllegalByteSequence()

		case 9:

			variant = MakeErrorCodeInProgress()

		case 10:

			variant = MakeErrorCodeInterrupted()

		case 11:

			variant = MakeErrorCodeInvalid()

		case 12:

			variant = MakeErrorCodeIo()

		case 13:

			variant = MakeErrorCodeIsDirectory()

		case 14:

			variant = MakeErrorCodeLoop()

		case 15:

			variant = MakeErrorCodeTooManyLinks()

		case 16:

			variant = MakeErrorCodeMessageSize()

		case 17:

			variant = MakeErrorCodeNameTooLong()

		case 18:

			variant = MakeErrorCodeNoDevice()

		case 19:

			variant = MakeErrorCodeNoEntry()

		case 20:

			variant = MakeErrorCodeNoLock()

		case 21:

			variant = MakeErrorCodeInsufficientMemory()

		case 22:

			variant = MakeErrorCodeInsufficientSpace()

		case 23:

			variant = MakeErrorCodeNotDirectory()

		case 24:

			variant = MakeErrorCodeNotEmpty()

		case 25:

			variant = MakeErrorCodeNotRecoverable()

		case 26:

			variant = MakeErrorCodeUnsupported()

		case 27:

			variant = MakeErrorCodeNoTty()

		case 28:

			variant = MakeErrorCodeNoSuchDevice()

		case 29:

			variant = MakeErrorCodeOverflow()

		case 30:

			variant = MakeErrorCodeNotPermitted()

		case 31:

			variant = MakeErrorCodePipe()

		case 32:

			variant = MakeErrorCodeReadOnly()

		case 33:

			variant = MakeErrorCodeInvalidSeek()

		case 34:

			variant = MakeErrorCodeTextFileBusy()

		case 35:

			variant = MakeErrorCodeCrossDevice()

		case 36:
			var option witTypes.Option[string]
			switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (2 * 4)))) {
			case 0:

				option = witTypes.None[string]()
			case 1:
				value := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (3 * 4)))))), *(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (4 * 4))))

				option = witTypes.Some[string](value)
			default:
				panic("unreachable")
			}

			variant = MakeErrorCodeOther(option)

		default:
			panic("unreachable")
		}

		result = witTypes.Err[witTypes.Unit, ErrorCode](variant)
	default:
		panic("unreachable")
	}

	return result

}

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][method]descriptor.unlink-file-at
func wasm_import_method_descriptor_unlink_file_at(arg0 int32, arg1 uintptr, arg2 uint32, arg3 uintptr) int32

func (self *Descriptor) UnlinkFileAt(path string) witTypes.Result[witTypes.Unit, ErrorCode] {
	pinner := &runtime.Pinner{}
	defer pinner.Unpin()

	returnArea := uintptr(witRuntime.Allocate(pinner, (5 * 4), 4))
	utf8 := unsafe.Pointer(unsafe.StringData(path))
	pinner.Pin(utf8)

	witAsync.SubtaskWait(uint32(wasm_import_method_descriptor_unlink_file_at((self).Handle(), uintptr(utf8), uint32(len(path)), returnArea)))
	var result witTypes.Result[witTypes.Unit, ErrorCode]
	switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 0))) {
	case 0:

		result = witTypes.Ok[witTypes.Unit, ErrorCode](witTypes.Unit{})
	case 1:
		var variant ErrorCode
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 4))) {
		case 0:

			variant = MakeErrorCodeAccess()

		case 1:

			variant = MakeErrorCodeAlready()

		case 2:

			variant = MakeErrorCodeBadDescriptor()

		case 3:

			variant = MakeErrorCodeBusy()

		case 4:

			variant = MakeErrorCodeDeadlock()

		case 5:

			variant = MakeErrorCodeQuota()

		case 6:

			variant = MakeErrorCodeExist()

		case 7:

			variant = MakeErrorCodeFileTooLarge()

		case 8:

			variant = MakeErrorCodeIllegalByteSequence()

		case 9:

			variant = MakeErrorCodeInProgress()

		case 10:

			variant = MakeErrorCodeInterrupted()

		case 11:

			variant = MakeErrorCodeInvalid()

		case 12:

			variant = MakeErrorCodeIo()

		case 13:

			variant = MakeErrorCodeIsDirectory()

		case 14:

			variant = MakeErrorCodeLoop()

		case 15:

			variant = MakeErrorCodeTooManyLinks()

		case 16:

			variant = MakeErrorCodeMessageSize()

		case 17:

			variant = MakeErrorCodeNameTooLong()

		case 18:

			variant = MakeErrorCodeNoDevice()

		case 19:

			variant = MakeErrorCodeNoEntry()

		case 20:

			variant = MakeErrorCodeNoLock()

		case 21:

			variant = MakeErrorCodeInsufficientMemory()

		case 22:

			variant = MakeErrorCodeInsufficientSpace()

		case 23:

			variant = MakeErrorCodeNotDirectory()

		case 24:

			variant = MakeErrorCodeNotEmpty()

		case 25:

			variant = MakeErrorCodeNotRecoverable()

		case 26:

			variant = MakeErrorCodeUnsupported()

		case 27:

			variant = MakeErrorCodeNoTty()

		case 28:

			variant = MakeErrorCodeNoSuchDevice()

		case 29:

			variant = MakeErrorCodeOverflow()

		case 30:

			variant = MakeErrorCodeNotPermitted()

		case 31:

			variant = MakeErrorCodePipe()

		case 32:

			variant = MakeErrorCodeReadOnly()

		case 33:

			variant = MakeErrorCodeInvalidSeek()

		case 34:

			variant = MakeErrorCodeTextFileBusy()

		case 35:

			variant = MakeErrorCodeCrossDevice()

		case 36:
			var option witTypes.Option[string]
			switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (2 * 4)))) {
			case 0:

				option = witTypes.None[string]()
			case 1:
				value := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (3 * 4)))))), *(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (4 * 4))))

				option = witTypes.Some[string](value)
			default:
				panic("unreachable")
			}

			variant = MakeErrorCodeOther(option)

		default:
			panic("unreachable")
		}

		result = witTypes.Err[witTypes.Unit, ErrorCode](variant)
	default:
		panic("unreachable")
	}

	return result

}

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][method]descriptor.is-same-object
func wasm_import_method_descriptor_is_same_object(arg0 int32, arg1 int32, arg2 uintptr) int32

func (self *Descriptor) IsSameObject(other *Descriptor) bool {
	pinner := &runtime.Pinner{}
	defer pinner.Unpin()

	returnArea := uintptr(witRuntime.Allocate(pinner, 1, 1))

	witAsync.SubtaskWait(uint32(wasm_import_method_descriptor_is_same_object((self).Handle(), (other).Handle(), returnArea)))

	return (uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 0))) != 0)

}

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][method]descriptor.metadata-hash
func wasm_import_method_descriptor_metadata_hash(arg0 int32, arg1 uintptr) int32

func (self *Descriptor) MetadataHash() witTypes.Result[MetadataHashValue, ErrorCode] {
	pinner := &runtime.Pinner{}
	defer pinner.Unpin()

	returnArea := uintptr(witRuntime.Allocate(pinner, (8 + 4*4), 8))

	witAsync.SubtaskWait(uint32(wasm_import_method_descriptor_metadata_hash((self).Handle(), returnArea)))
	var result witTypes.Result[MetadataHashValue, ErrorCode]
	switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 0))) {
	case 0:

		result = witTypes.Ok[MetadataHashValue, ErrorCode](MetadataHashValue{uint64(*(*int64)(unsafe.Add(unsafe.Pointer(returnArea), 8))), uint64(*(*int64)(unsafe.Add(unsafe.Pointer(returnArea), 16)))})
	case 1:
		var variant ErrorCode
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 8))) {
		case 0:

			variant = MakeErrorCodeAccess()

		case 1:

			variant = MakeErrorCodeAlready()

		case 2:

			variant = MakeErrorCodeBadDescriptor()

		case 3:

			variant = MakeErrorCodeBusy()

		case 4:

			variant = MakeErrorCodeDeadlock()

		case 5:

			variant = MakeErrorCodeQuota()

		case 6:

			variant = MakeErrorCodeExist()

		case 7:

			variant = MakeErrorCodeFileTooLarge()

		case 8:

			variant = MakeErrorCodeIllegalByteSequence()

		case 9:

			variant = MakeErrorCodeInProgress()

		case 10:

			variant = MakeErrorCodeInterrupted()

		case 11:

			variant = MakeErrorCodeInvalid()

		case 12:

			variant = MakeErrorCodeIo()

		case 13:

			variant = MakeErrorCodeIsDirectory()

		case 14:

			variant = MakeErrorCodeLoop()

		case 15:

			variant = MakeErrorCodeTooManyLinks()

		case 16:

			variant = MakeErrorCodeMessageSize()

		case 17:

			variant = MakeErrorCodeNameTooLong()

		case 18:

			variant = MakeErrorCodeNoDevice()

		case 19:

			variant = MakeErrorCodeNoEntry()

		case 20:

			variant = MakeErrorCodeNoLock()

		case 21:

			variant = MakeErrorCodeInsufficientMemory()

		case 22:

			variant = MakeErrorCodeInsufficientSpace()

		case 23:

			variant = MakeErrorCodeNotDirectory()

		case 24:

			variant = MakeErrorCodeNotEmpty()

		case 25:

			variant = MakeErrorCodeNotRecoverable()

		case 26:

			variant = MakeErrorCodeUnsupported()

		case 27:

			variant = MakeErrorCodeNoTty()

		case 28:

			variant = MakeErrorCodeNoSuchDevice()

		case 29:

			variant = MakeErrorCodeOverflow()

		case 30:

			variant = MakeErrorCodeNotPermitted()

		case 31:

			variant = MakeErrorCodePipe()

		case 32:

			variant = MakeErrorCodeReadOnly()

		case 33:

			variant = MakeErrorCodeInvalidSeek()

		case 34:

			variant = MakeErrorCodeTextFileBusy()

		case 35:

			variant = MakeErrorCodeCrossDevice()

		case 36:
			var option witTypes.Option[string]
			switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (8 + 1*4)))) {
			case 0:

				option = witTypes.None[string]()
			case 1:
				value := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (8 + 2*4)))))), *(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (8 + 3*4))))

				option = witTypes.Some[string](value)
			default:
				panic("unreachable")
			}

			variant = MakeErrorCodeOther(option)

		default:
			panic("unreachable")
		}

		result = witTypes.Err[MetadataHashValue, ErrorCode](variant)
	default:
		panic("unreachable")
	}

	return result

}

//go:wasmimport wasi:filesystem/types@0.3.0 [async-lower][method]descriptor.metadata-hash-at
func wasm_import_method_descriptor_metadata_hash_at(arg0 int32, arg1 int32, arg2 uintptr, arg3 uint32, arg4 uintptr) int32

func (self *Descriptor) MetadataHashAt(pathFlags PathFlags, path string) witTypes.Result[MetadataHashValue, ErrorCode] {
	pinner := &runtime.Pinner{}
	defer pinner.Unpin()

	returnArea := uintptr(witRuntime.Allocate(pinner, (8 + 4*4), 8))
	utf8 := unsafe.Pointer(unsafe.StringData(path))
	pinner.Pin(utf8)

	witAsync.SubtaskWait(uint32(wasm_import_method_descriptor_metadata_hash_at((self).Handle(), int32(pathFlags), uintptr(utf8), uint32(len(path)), returnArea)))
	var result witTypes.Result[MetadataHashValue, ErrorCode]
	switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 0))) {
	case 0:

		result = witTypes.Ok[MetadataHashValue, ErrorCode](MetadataHashValue{uint64(*(*int64)(unsafe.Add(unsafe.Pointer(returnArea), 8))), uint64(*(*int64)(unsafe.Add(unsafe.Pointer(returnArea), 16)))})
	case 1:
		var variant ErrorCode
		switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), 8))) {
		case 0:

			variant = MakeErrorCodeAccess()

		case 1:

			variant = MakeErrorCodeAlready()

		case 2:

			variant = MakeErrorCodeBadDescriptor()

		case 3:

			variant = MakeErrorCodeBusy()

		case 4:

			variant = MakeErrorCodeDeadlock()

		case 5:

			variant = MakeErrorCodeQuota()

		case 6:

			variant = MakeErrorCodeExist()

		case 7:

			variant = MakeErrorCodeFileTooLarge()

		case 8:

			variant = MakeErrorCodeIllegalByteSequence()

		case 9:

			variant = MakeErrorCodeInProgress()

		case 10:

			variant = MakeErrorCodeInterrupted()

		case 11:

			variant = MakeErrorCodeInvalid()

		case 12:

			variant = MakeErrorCodeIo()

		case 13:

			variant = MakeErrorCodeIsDirectory()

		case 14:

			variant = MakeErrorCodeLoop()

		case 15:

			variant = MakeErrorCodeTooManyLinks()

		case 16:

			variant = MakeErrorCodeMessageSize()

		case 17:

			variant = MakeErrorCodeNameTooLong()

		case 18:

			variant = MakeErrorCodeNoDevice()

		case 19:

			variant = MakeErrorCodeNoEntry()

		case 20:

			variant = MakeErrorCodeNoLock()

		case 21:

			variant = MakeErrorCodeInsufficientMemory()

		case 22:

			variant = MakeErrorCodeInsufficientSpace()

		case 23:

			variant = MakeErrorCodeNotDirectory()

		case 24:

			variant = MakeErrorCodeNotEmpty()

		case 25:

			variant = MakeErrorCodeNotRecoverable()

		case 26:

			variant = MakeErrorCodeUnsupported()

		case 27:

			variant = MakeErrorCodeNoTty()

		case 28:

			variant = MakeErrorCodeNoSuchDevice()

		case 29:

			variant = MakeErrorCodeOverflow()

		case 30:

			variant = MakeErrorCodeNotPermitted()

		case 31:

			variant = MakeErrorCodePipe()

		case 32:

			variant = MakeErrorCodeReadOnly()

		case 33:

			variant = MakeErrorCodeInvalidSeek()

		case 34:

			variant = MakeErrorCodeTextFileBusy()

		case 35:

			variant = MakeErrorCodeCrossDevice()

		case 36:
			var option witTypes.Option[string]
			switch uint8(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (8 + 1*4)))) {
			case 0:

				option = witTypes.None[string]()
			case 1:
				value := unsafe.String((*uint8)(unsafe.Pointer(uintptr(*(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (8 + 2*4)))))), *(*uint32)(unsafe.Add(unsafe.Pointer(returnArea), (8 + 3*4))))

				option = witTypes.Some[string](value)
			default:
				panic("unreachable")
			}

			variant = MakeErrorCodeOther(option)

		default:
			panic("unreachable")
		}

		result = witTypes.Err[MetadataHashValue, ErrorCode](variant)
	default:
		panic("unreachable")
	}

	return result

}
